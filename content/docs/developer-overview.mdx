# Developer Reference

This section is the precise contract reference for everyone integrating with Code UX — whether you are wiring it into an MCP client, building a dashboard plugin, or extending the engine.

If you are looking for narrative explanations, the [User Guide](/docs/user-overview) is friendlier. If you want internals, see [Architecture](/docs/architecture-overview).

## Sections

| # | Page | Purpose |
| --- | --- | --- |
| 1 | [MCP tools](/docs/developer-mcp-tools) | The per-domain `manage_*` tools and `search_knowledge`, with schemas |
| 2 | [Management actions](/docs/developer-management-actions) | Every action in every management domain |
| 3 | [HTTP API reference](/docs/developer-http-api) | All REST endpoints exposed by the dashboard server |
| 4 | [Realtime WebSocket protocol](/docs/developer-websocket-realtime) | The live update stream protocol |
| 5 | [Configuration & CLI](/docs/developer-configuration) | Every CLI flag, env var, and config search rule |
| 6 | [Settings schema reference](/docs/developer-settings-reference) | The full settings tree |
| 7 | [Sprint and subtask file format](/docs/developer-sprint-format) | On-disk markdown format with YAML frontmatter |
| 8 | [Building from source](/docs/developer-building-from-source) | Build, link, run from a clone |
| 9 | [Testing & quality gates](/docs/developer-testing) | Vitest, coverage thresholds, CI gates |

## Versioning

This reference tracks the current release line. Breaking changes follow semver — minor versions add tools / fields without removing them, major versions may remove or restructure.

The MCP server name is `code-ux` and the version matches the installed package. Capabilities advertised at `initialize`: `tools`, `resources`, `prompts`.

## Stable vs experimental

Unless explicitly marked `experimental` or `deprecated`, every contract documented here is stable for the 0.x series. Experimental surfaces:

- The `preview` management domain (interface may evolve as Docker tooling matures).
- The `WORKER` invocation routing profile.

