--- SprintsPage.tsx
+++ SprintsPage.tsx
@@ -200,8 +201,16 @@
     }
   });

+  const [testDialogOpen, setTestDialogOpen] = useState(true);
   return (
     <div className="flex flex-col h-[calc(100vh-64px)] overflow-hidden">
+      <ConfirmDialog
+        isOpen={testDialogOpen}
+        options={{ title: "Test Confirm", body: "This is a test dialog.", confirmLabel: "Submit", cancelLabel: "Dismiss" }}
+        onConfirm={() => new Promise(resolve => setTimeout(resolve, 500))}
+        onCancel={() => setTestDialogOpen(false)}
+      />
+
       {/* Top Row: Quick Filters */}
       <div className="flex-none p-3 lg:p-4 border-b border-black/[0.06] dark:border-white/[0.06]">
         <div className="max-w-[1920px] mx-auto w-full flex flex-col sm:flex-row sm:items-center justify-between gap-4">
