dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:160:      label: locale === "de" ? german[0] : meta.label,
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:164:      detail: locale === "de" ? german[1] : meta.detail,
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:189:      label: locale === "de" ? "Kein Quellensignal" : "No source signal",
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:190:      detail: locale === "de" ? "Keine Aufrufquellenzähler" : "No invocation source counters",
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:200:      label: locale === "de" ? "Gemeldet" : "Reported",
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:201:      detail: `${new Intl.NumberFormat(locale).format(reported)} ${locale === "de" ? "gemeldete Aufrufe" : "reported calls"}`,
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:211:      label: locale === "de" ? "Nicht verfügbar" : "Unavailable",
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:212:      detail: `${new Intl.NumberFormat(locale).format(failureRiskCount)} ${locale === "de" ? "nicht verfügbar oder nicht unterstützt" : "unavailable or unsupported"}`,
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:222:      label: locale === "de" ? "Gemischte Quellen" : "Mixed sources",
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:223:      detail: `${new Intl.NumberFormat(locale).format(reported)} ${locale === "de" ? "gemeldet" : "reported"} · ${new Intl.NumberFormat(locale).format(fallbackCount)} ${locale === "de" ? "Ersatzwerte" : "fallback"} · ${new Intl.NumberFormat(locale).format(failureRiskCount)} ${locale === "de" ? "nicht verfügbar" : "unavailable"}`,
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:233:      label: reported > 0 ? (locale === "de" ? "Gemeldet + Ersatzwerte" : "Reported + fallback") : (locale === "de" ? "Geschätzt" : "Estimated"),
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:234:      detail: `${new Intl.NumberFormat(locale).format(reported)} ${locale === "de" ? "gemeldet" : "reported"} · ${new Intl.NumberFormat(locale).format(fallbackCount)} ${locale === "de" ? "Ersatzwerte" : "fallback"}`,
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:243:    label: locale === "de" ? "Unbekannt" : "Unknown",
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:244:    detail: `${new Intl.NumberFormat(locale).format(unknown)} ${locale === "de" ? "unbekannte Aufrufe" : "unknown calls"}`,
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:397:            <div className="mt-1 break-words text-sm text-[color:var(--stats-detail-color)]">{provider.secondaryLabel ?? (locale === "de" ? "Keine sekundäre Bezeichnung" : "No secondary label")}</div>
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:403:                {locale === "de" ? ({ high: "Hohes Risiko", medium: "Mittleres Risiko", low: "Niedriges Risiko" } as const)[riskLevel] : `${riskLevel} risk`}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:413:            <span className="text-[color:var(--stats-label-color)]">{locale === "de" ? "Token" : "tokens"}</span>
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:419:            <span className="text-[color:var(--stats-label-color)]">{locale === "de" ? "Aufrufe" : "calls"}</span>
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:426:            <span className="text-[color:var(--stats-label-color)]">{locale === "de" ? "Kosten" : "cost"}</span>
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:433:          label={locale === "de" ? "Fehler" : "Failures"}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:435:          detail={`${formatNumber(row.completedCount)} ${locale === "de" ? "abgeschlossen" : "completed"} · ${formatNumber(row.runningCount)} ${locale === "de" ? "laufend" : "running"} · ${formatNumber(row.cancelledCount)} ${locale === "de" ? "abgebrochen" : "cancelled"}`}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:441:            <div className="text-[10px] font-bold uppercase tracking-[0.18em] text-[color:var(--stats-label-color)]">{locale === "de" ? "Erfolgsrate" : "Success Rate"}</div>
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:450:            {row.finishedCount > 0 ? `${formatNumber(row.finishedCount)} ${locale === "de" ? "beendete Modellläufe" : "finished model runs"}` : locale === "de" ? "Keine beendeten Modellläufe" : "No finished model runs"}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:454:          label={locale === "de" ? "Token-Volumen" : "Token Volume"}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:456:          detail={provider.usage.invocationCount > 0 ? `${formatTokens(Math.round(provider.usage.totalTokens / provider.usage.invocationCount), locale)}/${locale === "de" ? "Aufruf" : "call"}` : locale === "de" ? "Noch keine Aufrufe" : "No calls yet"}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:460:          label={locale === "de" ? "Kosten" : "Cost"}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:462:          detail={costPerCall !== null ? `${formatCost(costPerCall, locale)}/${locale === "de" ? "Aufruf" : "call"}` : locale === "de" ? "Kein Preissignal" : "No pricing signal"}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:469:          detail={costPerMillionTokens !== null ? (locale === "de" ? "Gemischter Token-Satz" : "Blended token rate") : (locale === "de" ? "Preise nicht verfügbar" : "Pricing unavailable")}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:473:          label={locale === "de" ? "Aktive Zeit" : "Active Time"}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:475:          detail={providerActiveVsWall !== null ? `${formatPercent(providerActiveVsWall * 100, locale)} ${locale === "de" ? "aktive Auslastung" : "active utilization"}` : locale === "de" ? "Gesamtzeit nicht erfasst" : "Wall time not tracked"}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:480:          label={locale === "de" ? "Dauer" : "Duration"}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:482:          detail={row.duration.sampleCount > 0 ? `${formatNumber(row.duration.sampleCount)} ${locale === "de" ? "Stichproben" : "samples"} · p95 ${formatStatsDuration(row.duration.p95Ms, locale)}` : locale === "de" ? "Keine Dauerstichproben" : "No duration samples"}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:487:          label={locale === "de" ? "Quellenvertrauen" : "Source Confidence"}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:551:              <div className="text-[10px] font-bold uppercase tracking-[0.18em] text-[color:var(--stats-label-color)]">{locale === "de" ? "Zuverlässigkeitsmodus" : "Reliability Mode"}</div>
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:552:              <div className="mt-1 break-words text-xl font-semibold tracking-tight text-[color:var(--stats-value-color)]">{locale === "de" ? "Provider-Vertrauen und Fehlerrisiko" : "Provider confidence & failure risk"}</div>
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:554:                {locale === "de" ? "Telemetrievertrauen, Quellenmix, Provider-Zustand, Ersatzwertnutzung und Fehlerdruck für den ausgewählten Statistikzeitraum." : "Telemetry confidence, source mix, provider health, fallback usage, and failure pressure for the selected Stats window."}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:560:              {sourceSummary.label} {locale === "de" ? "Vertrauen" : "confidence"}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:563:              {formatSuccessRate(successRate, locale)} {locale === "de" ? "Erfolg" : "success"}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:570:            label={locale === "de" ? "Telemetrievertrauen" : "Telemetry Confidence"}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:577:            label={locale === "de" ? "Ersatzwertnutzung" : "Fallback Usage"}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:579:            detail={`${formatNumber(sourceRows.find((row) => row.source === "estimated")!.count)} ${locale === "de" ? "geschätzt" : "estimated"} · ${formatNumber(sourceRows.find((row) => row.source === "unknown")!.count)} ${locale === "de" ? "unbekannt" : "unknown"}`}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:584:            label={locale === "de" ? "Fehlerdruck" : "Failure Pressure"}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:586:            detail={`${formatNumber(failureRiskCount)} ${locale === "de" ? "nicht verfügbare oder nicht unterstützte Quellenzähler" : "unavailable or unsupported source counts"}`}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:591:            label={locale === "de" ? "Provider-Abdeckung" : "Provider Coverage"}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:592:            value={providerRows.length > 0 ? `${formatNumber(providerRows.length)} Provider` : locale === "de" ? "Keine Provider" : "No providers"}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:593:            detail={highestRiskProvider ? `${locale === "de" ? "Höchstes Risiko" : "Highest risk"}: ${highestRiskProvider.provider.label}` : locale === "de" ? "Keine Provider-Telemetrie eingegangen" : "No provider telemetry landed"}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:597:            label={locale === "de" ? "Provider-Kosten" : "Provider Cost"}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:599:            detail={highestCostProvider && highestCostProvider.usage.totalCostUsd > 0 ? `${locale === "de" ? "Höchste Kosten" : "Highest cost"}: ${highestCostProvider.label}` : locale === "de" ? "Kein Preissignal" : "No pricing signal"}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:609:            title={locale === "de" ? "Telemetriequellenmix" : "Telemetry Source Mix"}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:610:            eyebrow={locale === "de" ? "Quellenvertrauen" : "Source Confidence"}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:611:            description={locale === "de" ? "Gemeldete, geschätzte, nicht verfügbare, nicht unterstützte und unbekannte Aufrufquellenzähler in diesem Zeitraum." : "Reported, estimated, unavailable, unsupported, and unknown invocation-source counts across this window."}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:613:            centerLabel={locale === "de" ? "Quellenzähler" : "source counts"}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:618:            <div className="text-[10px] font-bold uppercase tracking-[0.2em] text-[color:var(--stats-label-color)]">{locale === "de" ? "Quellenvertrauen" : "Source Confidence"}</div>
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:619:            <div className="mt-2 text-xl font-semibold tracking-tight text-[color:var(--stats-value-color)]">{locale === "de" ? "Telemetriequellenmix" : "Telemetry Source Mix"}</div>
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:621:              {locale === "de" ? "Gemeldete, geschätzte, nicht verfügbare, nicht unterstützte und unbekannte Aufrufquellenzähler in diesem Zeitraum." : "Reported, estimated, unavailable, unsupported, and unknown invocation-source counts across this window."}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:625:                title={locale === "de" ? "Keine Telemetriequellensegmente für diesen Zeitraum." : "No telemetry source segments for this window."}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:626:                detail={locale === "de" ? "Provider-Aufrufzahlen können dennoch vorhanden sein, aber der Snapshot enthält keine darstellbaren Quellensegmente." : "Provider invocation counts may still exist, but the snapshot did not include source-segment lanes to chart."}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:633:            title={locale === "de" ? "Provider-Anteil" : "Provider Share"}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:634:            eyebrow={locale === "de" ? "Volumenkontext" : "Volume Context"}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:635:            description={locale === "de" ? "Token-Volumen nach Provider neben Vertrauens- und Risikosignalen, damit Provider mit hohem Volumen leicht priorisiert werden können." : "Token volume by provider, shown beside confidence and risk signals so high-volume providers stay easy to triage."}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:637:            centerLabel={locale === "de" ? "Token-Volumen" : "token volume"}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:642:            <div className="text-[10px] font-bold uppercase tracking-[0.2em] text-[color:var(--stats-label-color)]">{locale === "de" ? "Volumenkontext" : "Volume Context"}</div>
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:643:            <div className="mt-2 text-xl font-semibold tracking-tight text-[color:var(--stats-value-color)]">{locale === "de" ? "Provider-Anteil" : "Provider Share"}</div>
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:645:              {locale === "de" ? "Das Token-Volumen nach Provider erscheint hier, wenn der ausgewählte Zeitraum Provider-Segmente enthält." : "Token volume by provider appears here when the selected window includes provider segments."}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:649:                title={locale === "de" ? "Keine Provider-Segmente für diesen Zeitraum." : "No provider segments for this window."}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:650:                detail={locale === "de" ? "Provider-Zuverlässigkeitskarten erscheinen weiterhin unten, wenn Provider-Zeilen ohne darstellbaren Token-Anteil vorhanden sind." : "Provider reliability cards will still appear below when provider rows exist without chartable token share."}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:660:            <div className={SECTION_TITLE_CLASS}>{locale === "de" ? "Vertrauensübersicht" : "Confidence Board"}</div>
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:662:              {locale === "de" ? "Aufrufquellenzähler werden getrennt, damit geschätzte und unbekannte Daten klar erkennbar sind, ohne Ersatzschätzungen als Fehler zu behandeln." : "Invocation-source counts are separated so estimated and unknown data are clear without treating fallback estimates as failures."}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:666:            {formatNumber(sourceAudit.total)} {locale === "de" ? "gezählt" : "counted"}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:677:            <div className={SECTION_TITLE_CLASS}>{locale === "de" ? "Provider-Aufschlüsselung" : "Provider Breakdown"}</div>
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:679:              {locale === "de" ? "Provider-Zeilen ordnen Fehlerrisiko, Quellenvertrauen, Token-Volumen, Latenz und Preissignale zur Priorisierung." : "Provider rows rank failure risk, source confidence, token volume, latency, and pricing signals for triage."}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:688:            title={locale === "de" ? "Keine Provider-Telemetrie für diesen Zeitraum." : "No provider telemetry for this window."}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:689:            detail={locale === "de" ? "Provider-Zuverlässigkeit erscheint, sobald erfasste Aufrufe Provider-Nutzung oder Statuszusammenfassungen enthalten." : "Provider reliability appears after tracked invocations record provider usage or status summaries."}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:701:          <div className={SECTION_TITLE_CLASS}>{locale === "de" ? "Prüfhinweise" : "Audit Notes"}</div>
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:705:            <div className="text-sm font-semibold text-[color:var(--stats-value-color)]">{locale === "de" ? "Ersatzwertmix" : "Fallback mix"}</div>
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:708:                ? locale === "de" ? `${formatNumber(fallbackCount)} Aufrufe nutzten geschätzte oder unbekannte Quellenzähler. Schätzungen bleiben nutzbar, sind aber weniger präzise als Provider-Meldungen.` : `${formatNumber(fallbackCount)} invocations relied on estimated or unknown source counters. Estimates remain usable, but precision is lower than provider-reported counts.`
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:709:                : locale === "de" ? "In diesem Zeitraum wurden keine geschätzten oder unbekannten Aufrufquellenzähler erfasst." : "No estimated or unknown invocation-source counts were recorded in this window."}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:713:            <div className="text-sm font-semibold text-[color:var(--stats-value-color)]">{locale === "de" ? "Fehlerrisiko" : "Failure risk"}</div>
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:716:                ? locale === "de" ? `${formatNumber(stats.statusCounts.failed)} Aufrufe schlugen fehl und ${formatNumber(failureRiskCount)} Quellenzähler waren nicht verfügbar oder nicht unterstützt.` : `${formatNumber(stats.statusCounts.failed)} invocations failed and ${formatNumber(failureRiskCount)} source counts were unavailable or unsupported.`
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:717:                : locale === "de" ? "In diesem Zeitraum wurden keine fehlgeschlagenen Aufrufe oder nicht verfügbaren Quellenzähler erfasst." : "No failed invocations or unavailable source counts were recorded in this window."}
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:721:            <div className="text-sm font-semibold text-[color:var(--stats-value-color)]">{locale === "de" ? "Dauerabdeckung" : "Duration coverage"}</div>
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:724:                ? locale === "de" ? `Die Latenz basiert auf ${formatNumber(stats.duration.sampleCount)} Stichproben: p50 ${formatStatsDuration(stats.duration.p50Ms, locale)}, p95 ${formatStatsDuration(stats.duration.p95Ms, locale)}.` : `Latency is backed by ${formatNumber(stats.duration.sampleCount)} samples: p50 ${formatStatsDuration(stats.duration.p50Ms, locale)}, p95 ${formatStatsDuration(stats.duration.p95Ms, locale)}.`
dashboard/src/v2/pages/stats/components/ReliabilityStudio.tsx:725:                : locale === "de" ? "Es wurden keine Dauerstichproben erfasst; die Latenz bleibt daher nicht verfügbar, statt abgeleitet zu werden." : "No duration samples were recorded, so latency remains unavailable rather than inferred."}
dashboard/src/v2/pages/stats/components/stats-ui-primitives.tsx:161:          {value === "all" ? (locale === "de" ? "Gesamte Zeit" : "All time") : value}
dashboard/src/v2/pages/stats/components/stats-ui-primitives.tsx:174:        {locale === "de" ? "Benutzerdefiniert" : "Custom"}
dashboard/src/v2/pages/stats/components/stats-ui-primitives.tsx:195:        {locale === "de" ? "Anwenden" : "Apply"}
dashboard/src/v2/pages/stats/components/stats-ui-primitives.tsx:214:    { id: "composition", label: locale === "de" ? "Zusammensetzung" : "Composition", accessibleLabel: locale === "de" ? "Zusammensetzung" : "Composition", icon: PieChart },
dashboard/src/v2/pages/stats/components/stats-ui-primitives.tsx:215:    { id: "cost", label: locale === "de" ? "Kosten" : "Cost", accessibleLabel: locale === "de" ? "Kosten" : "Cost", icon: Coins },
dashboard/src/v2/pages/stats/components/stats-ui-primitives.tsx:216:    { id: "models", label: locale === "de" ? "Modelle" : "Models", accessibleLabel: locale === "de" ? "Modelle" : "Models", icon: Cpu },
dashboard/src/v2/pages/stats/components/stats-ui-primitives.tsx:217:    { id: "reliability", label: locale === "de" ? "Provider" : "Providers", accessibleLabel: locale === "de" ? "Provider" : "Providers", icon: ShieldCheck },
dashboard/src/v2/pages/stats/components/stats-ui-primitives.tsx:261:        {locale === "de" ? "Ausgewählter Analysemodus" : "Selected analytics mode"}: {modes.find((mode) => mode.id === value)?.accessibleLabel ?? value}.
dashboard/src/v2/pages/stats/components/stats-ui-primitives.tsx:367:    ? locale === "de" ? `Eingabe ${formatTokens(input, locale)}; im Cache ${formatTokens(cached, locale)}; Ausgabe ${formatTokens(output, locale)}; Schlussfolgerung ${formatTokens(reasoning, locale)}; gesamt ${formatTokens(total, locale)}.` : `Input ${formatTokens(input, locale)}; cached ${formatTokens(cached, locale)}; output ${formatTokens(output, locale)}; reasoning ${formatTokens(reasoning, locale)}; total ${formatTokens(total, locale)}.`
dashboard/src/v2/pages/stats/components/stats-ui-primitives.tsx:368:    : locale === "de" ? "Keine Token-Flussdaten verfügbar." : "No token flow data available.";
dashboard/src/v2/pages/stats/components/stats-ui-primitives.tsx:378:      {inPct > 0 && <div aria-hidden="true" className="h-full bg-[color:var(--stats-signal-text)] motion-safe:transition-all motion-safe:duration-500" style={{ width: `${inPct}%` }} title={`${locale === "de" ? "Eingabe" : "Input"}: ${formatNumber(inPct, { minimumFractionDigits: 1, maximumFractionDigits: 1 })}%`} />}
dashboard/src/v2/pages/stats/components/stats-ui-primitives.tsx:379:      {cachedPct > 0 && <div aria-hidden="true" className="h-full bg-[color:var(--stats-accent-cyan)] motion-safe:transition-all motion-safe:duration-500" style={{ width: `${cachedPct}%` }} title={`${locale === "de" ? "Im Cache" : "Cached"}: ${formatNumber(cachedPct, { minimumFractionDigits: 1, maximumFractionDigits: 1 })}%`} />}
dashboard/src/v2/pages/stats/components/stats-ui-primitives.tsx:380:      {outPct > 0 && <div aria-hidden="true" className="h-full bg-[color:var(--stats-warning-text)] motion-safe:transition-all motion-safe:duration-500" style={{ width: `${outPct}%` }} title={`${locale === "de" ? "Ausgabe" : "Output"}: ${formatNumber(outPct, { minimumFractionDigits: 1, maximumFractionDigits: 1 })}%`} />}
dashboard/src/v2/pages/stats/components/stats-ui-primitives.tsx:381:      {reasonPct > 0 && <div aria-hidden="true" className="h-full bg-[color:var(--stats-negative-text)] motion-safe:transition-all motion-safe:duration-500" style={{ width: `${reasonPct}%` }} title={`${locale === "de" ? "Schlussfolgerung" : "Reasoning"}: ${formatNumber(reasonPct, { minimumFractionDigits: 1, maximumFractionDigits: 1 })}%`} />}
dashboard/src/v2/pages/stats/components/stats-ui-primitives.tsx:393:    ? locale === "de" ? `Codeänderungsmix: ${formatNumber(insertions)} Einfügungen, ${formatNumber(deletions)} Löschungen, ${formatNumber(total)} geänderte Zeilen insgesamt.` : `Code churn mix: ${formatNumber(insertions)} insertions, ${formatNumber(deletions)} deletions, ${formatNumber(total)} total changed lines.`
dashboard/src/v2/pages/stats/components/stats-ui-primitives.tsx:394:    : locale === "de" ? "Keine Codeänderungsdaten verfügbar." : "No code churn data available.";
dashboard/src/v2/pages/stats/components/stats-ui-primitives.tsx:402:      {inPct > 0 && <div aria-hidden="true" className="h-full bg-[color:var(--stats-positive-text)] motion-safe:transition-all motion-safe:duration-500" style={{ width: `${inPct}%` }} title={`${locale === "de" ? "Einfügungen" : "Insertions"}: ${formatNumber(inPct, { minimumFractionDigits: 1, maximumFractionDigits: 1 })}%`} />}
dashboard/src/v2/pages/stats/components/stats-ui-primitives.tsx:403:      {delPct > 0 && <div aria-hidden="true" className="h-full bg-[color:var(--stats-negative-text)] motion-safe:transition-all motion-safe:duration-500" style={{ width: `${delPct}%` }} title={`${locale === "de" ? "Löschungen" : "Deletions"}: ${formatNumber(delPct, { minimumFractionDigits: 1, maximumFractionDigits: 1 })}%`} />}
dashboard/src/v2/pages/stats/components/stats-ui-primitives.tsx:555:                  {activeSegment ? `${formatPercent(activeSegment.share, locale)} ${locale === "de" ? "des sichtbaren Volumens" : "of visible volume"}` : `${formatNumber(segments.length)} ${locale === "de" ? "Bereiche" : "lanes"}`}
dashboard/src/v2/pages/stats/components/stats-ui-primitives.tsx:563:                {locale === "de" ? "In dieser Zusammensetzung ist noch keine Telemetrie eingegangen." : "No telemetry landed in this composition yet."}
dashboard/src/v2/pages/stats/components/stats-ui-primitives.tsx:582:                        {formatPercent(segment.share, locale)} {locale === "de" ? "des sichtbaren Volumens" : "of visible volume"}
dashboard/src/v2/pages/stats/components/stats-ui-primitives.tsx:587:                      <div className="text-[10px] font-bold uppercase tracking-[0.14em] text-[color:var(--stats-label-color)]">{locale === "de" ? "Token" : "tokens"}</div>
dashboard/src/v2/pages/stats/components/stats-ui-primitives.tsx:624:        {locale === "de" ? "Keine Zweckdaten für diesen Zeitraum." : "No purpose data for this window."}
dashboard/src/v2/pages/stats/components/stats-ui-primitives.tsx:652:                  {formatNumber(purpose.usage.invocationCount)} {locale === "de" ? "Aufrufe" : "calls"} / {formatStatsDuration(purpose.usage.activeTimeMs, locale)} {locale === "de" ? "aktiv" : "active"}
dashboard/src/v2/pages/stats/components/stats-ui-primitives.tsx:664:                    {tokenShare !== null ? `${formatPercent(tokenShare, locale)} ${locale === "de" ? "Token-Anteil" : "token share"}` : locale === "de" ? "Kein Token-Anteil" : "No token share"}
dashboard/src/v2/pages/stats/components/stats-ui-primitives.tsx:669:                    {locale === "de" ? "Dominant" : "Dominant"}
dashboard/src/v2/pages/stats/components/stats-ui-primitives.tsx:711:  const directionLabel = active && direction ? `, ${locale === "de" ? "sortiert" : "sorted"} ${direction === "asc" ? (locale === "de" ? "aufsteigend" : "ascending") : (locale === "de" ? "absteigend" : "descending")}` : locale === "de" ? ", nicht sortiert" : ", not sorted";
dashboard/src/v2/pages/stats/components/StatsPageHero.tsx:69:  if (sec < 60) return locale === "de" ? "gerade eben" : "just now";
dashboard/src/v2/pages/stats/components/StatsPageHero.tsx:71:  if (min < 60) return locale === "de" ? `vor ${min} Min.` : `${min} min ago`;
dashboard/src/v2/pages/stats/components/StatsPageHero.tsx:73:  if (hr < 24) return locale === "de" ? `vor ${hr} Std.` : `${hr} hr ago`;
dashboard/src/v2/pages/stats/components/StatsPageHero.tsx:75:  return locale === "de" ? `vor ${day} Tag${day === 1 ? "" : "en"}` : `${day} day${day > 1 ? "s" : ""} ago`;
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:47:  const displayKind = locale === "de" ? (kindLabel === "tasks" ? "Aufgaben" : "Sprints") : kindLabel;
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:127:              {locale === "de" ? `${displayKind} nach Codeänderungen, geöffneten PRs und zusammengeführten Änderungen durchsuchen, sortieren und vergleichen.` : `Search, sort, and compare ${kindLabel} by code churn, PRs opened, and changes merged.`}
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:131:            {formatNumber(filteredItems.length)} {locale === "de" ? "sichtbar" : "visible"} / {formatNumber(items.length)} {locale === "de" ? "gesamt" : "total"}
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:137:            <div className="text-[10px] font-bold uppercase tracking-[0.16em] text-[color:var(--stats-label-color)]">{locale === "de" ? "Einfügungen" : "Insertions"}</div>
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:139:            <div className="mt-1 text-[10px] font-bold uppercase tracking-[0.12em] text-[color:var(--stats-label-color)]">{locale === "de" ? "Zeilen hinzugefügt" : "lines added"}</div>
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:142:            <div className="text-[10px] font-bold uppercase tracking-[0.16em] text-[color:var(--stats-label-color)]">{locale === "de" ? "Löschungen" : "Deletions"}</div>
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:144:            <div className="mt-1 text-[10px] font-bold uppercase tracking-[0.12em] text-[color:var(--stats-label-color)]">{locale === "de" ? "Zeilen entfernt" : "lines removed"}</div>
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:147:            <div className="text-[10px] font-bold uppercase tracking-[0.16em] text-[color:var(--stats-label-color)]">{locale === "de" ? "Dateien" : "Files"}</div>
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:149:            <div className="mt-1 text-[10px] font-bold uppercase tracking-[0.12em] text-[color:var(--stats-label-color)]">{locale === "de" ? "geändert" : "changed"}</div>
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:154:            <div className="mt-1 text-[10px] font-bold uppercase tracking-[0.12em] text-[color:var(--stats-label-color)]">{locale === "de" ? "geöffnet" : "opened"}</div>
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:157:            <div className="text-[10px] font-bold uppercase tracking-[0.16em] text-[color:var(--stats-label-color)]">{locale === "de" ? "Zusammengeführt" : "Merged"}</div>
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:159:            <div className="mt-1 text-[10px] font-bold uppercase tracking-[0.12em] text-[color:var(--stats-label-color)]">{formatPercent(mergeRate, locale)} {locale === "de" ? "Merge-Rate" : "merge rate"}</div>
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:162:            <div className="text-[10px] font-bold uppercase tracking-[0.16em] text-[color:var(--stats-label-color)]">{locale === "de" ? "Sichtbare Änderungen" : "Visible Churn"}</div>
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:164:            <div className="mt-1 text-[10px] font-bold uppercase tracking-[0.12em] text-[color:var(--stats-label-color)]">{formatPercent(filteredShare, locale)} {locale === "de" ? "in Ansicht" : "in view"}</div>
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:171:              {locale === "de" ? `Git-Telemetrie für ${displayKind} durchsuchen` : `Search ${kindLabel} git telemetry`}
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:179:              placeholder={locale === "de" ? `${displayKind} durchsuchen` : `Search ${kindLabel}`}
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:186:                aria-label={locale === "de" ? "Suche leeren" : "Clear search"}
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:195:              ["insertions", locale === "de" ? "Einfügungen" : "Insertions"],
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:196:              ["deletions", locale === "de" ? "Löschungen" : "Deletions"],
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:197:              ["filesChanged", locale === "de" ? "Dateien" : "Files"],
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:199:              ["mergedCount", locale === "de" ? "Zusammengeführt" : "Merged"],
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:217:                <div>{locale === "de" ? `Keine ${displayKind} passen zu „${query.trim()}“. ` : `No ${kindLabel} match “${query.trim()}”.`}</div>
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:223:                  {locale === "de" ? "Suche leeren" : "Clear search"}
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:237:                  <div key={item.id} className={LEDGER_ROW_MODERN_CLASS} aria-label={locale === "de" ? `${item.label}, Git-Telemetriezeile` : `${item.label} git telemetry row`}>
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:253:                                {formatNumber(item.metrics.filesChanged)} {locale === "de" ? "Dateien geändert" : "Files Changed"}
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:260:                            <div className="text-[10px] font-bold uppercase tracking-[0.16em] text-[color:var(--stats-label-color)]">{locale === "de" ? "Codeänderungen" : "Code Churn"}</div>
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:273:                            <div className="text-[10px] font-bold uppercase tracking-[0.16em] text-[color:var(--stats-label-color)]">{locale === "de" ? "Zusammengeführt" : "Merged"}</div>
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:277:                            <div className="text-[10px] font-bold uppercase tracking-[0.16em] text-[color:var(--stats-label-color)]">{locale === "de" ? "Dateien" : "Files"}</div>
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:281:                            <div className="text-[10px] font-bold uppercase tracking-[0.16em] text-[color:var(--stats-label-color)]">{locale === "de" ? "Anteil" : "Share"}</div>
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:285:                            <div className="text-[10px] font-bold uppercase tracking-[0.16em] text-[color:var(--stats-label-color)]">{locale === "de" ? "Führung" : "Leader"}</div>
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:293:                          <span>{locale === "de" ? "Änderungsmix" : "Churn mix"}</span>
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:294:                          <span>{locale === "de" ? "Einfügungen gegenüber Löschungen" : "Insertions vs deletions"}</span>
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:308:                            <TokenChip icon={PlusSquare} label={locale === "de" ? "Einfügungen" : "Insertions"} value={`+${formatNumber(item.metrics.insertions)}`} tone={STATUS_TONE_CLASS.positive} />
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:309:                            <TokenChip icon={MinusSquare} label={locale === "de" ? "Löschungen" : "Deletions"} value={`-${formatNumber(item.metrics.deletions)}`} tone={STATUS_TONE_CLASS.negative} />
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:310:                            <TokenChip icon={FileEdit} label={locale === "de" ? "Dateien" : "Files"} value={formatNumber(item.metrics.filesChanged)} tone={STATUS_TONE_CLASS.cyan} />
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:312:                            <TokenChip icon={GitMerge} label={locale === "de" ? "Zusammengeführt" : "Merged"} value={formatNumber(item.metrics.mergedCount)} tone={STATUS_TONE_CLASS.signal} />
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:313:                            <TokenChip icon={AlertTriangle} label={locale === "de" ? "Konflikte" : "Conflicts"} value={formatNumber(getMetricCount(item.metrics.mergeConflictCount))} tone={STATUS_TONE_CLASS.warning} />
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:316:                            {formatPercent(shareOfTotal, locale)} {locale === "de" ? "der sichtbaren Änderungen" : "of visible churn"} · {formatPercent(shareOfLeader, locale)} {locale === "de" ? "der Führung" : "of leader"}
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:326:                  {locale === "de" ? "Weitere Telemetriebereiche werden geladen …" : "Loading more telemetry lanes..."}
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:376:          <div className="text-[10px] font-bold uppercase tracking-[0.18em] text-[color:var(--stats-label-color)]">{locale === "de" ? "Ranglistenübersicht" : "Ranking Snapshot"}</div>
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:377:          <div className="mt-2 text-sm font-bold text-[color:var(--stats-value-color)]">{locale === "de" ? "Zeitbereiche und führende Entitäten" : "Buckets and entity leaders"}</div>
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:380:          {formatNumber(rankedBuckets.length)} {locale === "de" ? "Zeitbereiche" : "buckets"}
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:386:          <div className="mb-2 text-[10px] font-bold uppercase tracking-[0.16em] text-[color:var(--stats-label-color)]">{locale === "de" ? "Aktivste Zeitbereiche" : "Busiest buckets"}</div>
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:398:                        {formatNumber(bucket.metrics.prCount)} PRs · {formatNumber(getMetricCount(bucket.metrics.mergeConflictCount))} {locale === "de" ? "Konflikte" : "conflicts"}
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:403:                      <div className="text-[10px] uppercase tracking-[0.16em] text-[color:var(--stats-label-color)]">{locale === "de" ? "Änderungen" : "churn"}</div>
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:409:                    <span>{formatNumber(bucket.metrics.filesChanged)} {locale === "de" ? "Dateien" : "files"}</span>
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:410:                    <span>{formatNumber(bucket.metrics.mergedCount)} {locale === "de" ? "zusammengeführt" : "merged"}</span>
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:417:                {locale === "de" ? "Noch keine Zeitbereichsdaten verfügbar." : "No bucket data is available yet."}
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:424:          <div className="mb-2 text-[10px] font-bold uppercase tracking-[0.16em] text-[color:var(--stats-label-color)]">{locale === "de" ? "Führende Entitäten" : "Entity leaders"}</div>
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:426:            <div className="text-[10px] font-bold uppercase tracking-[0.14em] text-[color:var(--stats-label-color)]">{locale === "de" ? "Führende Aufgabe" : "Top task"}</div>
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:427:            <div className="mt-1 text-sm font-bold text-[color:var(--stats-value-color)]">{topTask ? topTask.label : locale === "de" ? "Noch keine führende Aufgabe" : "No task leader yet"}</div>
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:430:                {formatNumber(topTask.metrics.prCount)} PRs · {formatNumber(topTask.metrics.mergedCount)} {locale === "de" ? "zusammengeführt" : "merged"} · {formatNumber(getMetricCount(topTask.metrics.mergeConflictCount))} {locale === "de" ? "Konflikte" : "conflicts"}
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:435:            <div className="text-[10px] font-bold uppercase tracking-[0.14em] text-[color:var(--stats-label-color)]">{locale === "de" ? "Führender Sprint" : "Top sprint"}</div>
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:436:            <div className="mt-1 text-sm font-bold text-[color:var(--stats-value-color)]">{topSprint ? topSprint.label : locale === "de" ? "Noch kein führender Sprint" : "No sprint leader yet"}</div>
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:439:                {formatNumber(topSprint.metrics.prCount)} PRs · {formatNumber(topSprint.metrics.filesChanged)} {locale === "de" ? "Dateien geändert" : "files changed"}
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:460:        {locale === "de" ? "In diesem Zeitraum ist keine Git-Telemetrie verfügbar." : "No git telemetry available in this window."}
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:466:    { id: "tasks" as const, label: locale === "de" ? "Aufgabenrangliste" : "Task Leaderboard", count: gitStats.tasks.length, icon: ListTodo },
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:467:    { id: "sprints" as const, label: locale === "de" ? "Sprintrangliste" : "Sprint Leaderboard", count: gitStats.sprints.length, icon: Flag },
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:475:      <section className={`${PANEL_CLASS} p-5 md:p-6`} aria-label={locale === "de" ? "Git-Telemetrieübersicht" : "Git telemetry overview"}>
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:480:              {locale === "de" ? "Operatives Git-Ledger" : "Git Operational Ledger"}
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:483:              {locale === "de" ? "Codeänderungen, Pull Requests und Merge-Druck" : "Churn, pull requests, and merge pressure"}
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:486:              {locale === "de" ? "Git-Ausgaben von Aufgaben und Sprints nach Codeänderungen, geänderten Dateien, PR-Durchsatz, Merges und Konfliktsignalen prüfen." : "Review task and sprint git output by code churn, changed files, PR throughput, merges, and conflict signals."}
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:492:                <div className="text-[10px] font-bold uppercase tracking-[0.16em] text-[color:var(--stats-label-color)]">{locale === "de" ? "Gesamte Änderungen" : "Total Churn"}</div>
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:497:                {formatNumber(gitStats.tasks.length)} {locale === "de" ? "Aufgaben" : "tasks"} · {formatNumber(gitStats.sprints.length)} {text("sprints")}
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:504:              <span className={`px-2.5 py-1 text-[10px] font-bold uppercase tracking-[0.14em] text-[color:var(--stats-positive-text)] ${CHIP_CLASS}`}>+{formatNumber(gitStats.totals.insertions)} {locale === "de" ? "Einfügungen" : "insertions"}</span>
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:505:              <span className={`px-2.5 py-1 text-[10px] font-bold uppercase tracking-[0.14em] text-[color:var(--stats-negative-text)] ${CHIP_CLASS}`}>-{formatNumber(gitStats.totals.deletions)} {locale === "de" ? "Löschungen" : "deletions"}</span>
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:506:              <span className={`px-2.5 py-1 text-[10px] font-bold uppercase tracking-[0.14em] text-[color:var(--stats-warning-text)] ${CHIP_CLASS}`}>{formatNumber(conflictCount)} {locale === "de" ? "Konflikte" : "conflicts"}</span>
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:507:              <span className={`px-2.5 py-1 text-[10px] font-bold uppercase tracking-[0.14em] text-[color:var(--stats-signal-text)] ${CHIP_CLASS}`}>{formatPercent(mergeRate, locale)} {locale === "de" ? "Merge-Rate" : "merge rate"}</span>
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:513:      <section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3" aria-label={locale === "de" ? "Summen der Git-Telemetrie" : "Git telemetry totals"}>
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:516:          label={locale === "de" ? "Einfügungen" : "Insertions"}
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:518:          detail={`${formatNumber(totalChurn)} ${locale === "de" ? "gesamte Änderungen" : "total churn"}`}
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:523:          label={locale === "de" ? "Löschungen" : "Deletions"}
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:525:          detail={`${formatNumber(gitStats.totals.filesChanged)} ${locale === "de" ? "Dateien geändert" : "files changed"}`}
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:530:          label={locale === "de" ? "Geänderte Dateien" : "Files Changed"}
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:532:          detail={`${formatNumber(gitStats.totals.prCount)} ${locale === "de" ? "PRs im Bereich" : "PRs in scope"}`}
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:539:          detail={`${formatNumber(gitStats.totals.mergedCount)} ${locale === "de" ? "zusammengeführt" : "merged"}`}
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:546:          detail={`${formatNumber(conflictCount)} ${locale === "de" ? "Konflikte im Bereich" : "conflicts in scope"}`}
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:551:          label={locale === "de" ? "Merge-Konflikte" : "Merge Conflicts"}
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:553:          detail={`${formatPercent(gitStats.totals.prCount > 0 ? (conflictCount / gitStats.totals.prCount) * 100 : 0, locale)} ${locale === "de" ? "der PRs" : "of PRs"}`}
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:562:        aria-label={locale === "de" ? "Git-Telemetrieranglisten" : "Git telemetry leaderboards"}
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:623:            title={locale === "de" ? "Git-Telemetrie nach Aufgabe" : "Task Git Telemetry"}
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:624:            eyebrow={locale === "de" ? "Aufgaben-Git-Ledger" : "Task Git Ledger"}
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:627:            emptyLabel={locale === "de" ? "In diesem Zeitraum ist noch keine Git-Telemetrie für Aufgaben eingegangen." : "No task git telemetry landed in this window yet."}
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:631:            title={locale === "de" ? "Git-Telemetrie nach Sprint" : "Sprint Git Telemetry"}
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:632:            eyebrow={locale === "de" ? "Sprint-Git-Ledger" : "Sprint Git Ledger"}
dashboard/src/v2/pages/stats/components/GitTelemetryTab.tsx:635:            emptyLabel={locale === "de" ? "In diesem Zeitraum ist keine Git-Telemetrie für Sprints aktiv." : "No sprint git telemetry active in this window."}
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:88:          <div className="text-[10px] font-bold uppercase tracking-[0.14em] text-[color:var(--stats-label-color)]">{locale === "de" ? "Token-Fluss-Vergleich" : "Token flow comparison"}</div>
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:90:            {locale === "de" ? `Gefilterte ${kindLabel === "tasks" ? "Aufgaben" : "Sprints"} gegenüber dem führenden Bereich` : `Filtered ${kindLabel} vs leading lane`}
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:94:          {formatNumber(filteredItems.length)} {locale === "de" ? "sichtbar" : "visible"}
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:101:            <span>{locale === "de" ? "Gefilterter Mix" : "Filtered mix"}</span>
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:102:            <span>{formatTokens(totals.filteredTokens, locale)} {locale === "de" ? "Tokens gesamt" : "total tokens"}</span>
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:112:            {locale === "de" ? "Kombinierter gefilterter Durchsatz der aktuellen Ansicht." : "Combined filtered throughput across the current view."}
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:118:            <span>{locale === "de" ? "Führender Bereich" : "Top lane"}</span>
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:119:            <span>{topItem ? topItem.label : locale === "de" ? "Noch kein Bereich" : "No lane yet"}</span>
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:131:                {formatTokens(topItem.usage.totalTokens, locale)} {text("tokens")}, {formatPercent(totals.totalTokens > 0 ? (topItem.usage.totalTokens / totals.totalTokens) * 100 : 0, locale)} {locale === "de" ? "aller Tokens im Zeitraum." : "of all window tokens."}
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:136:              {locale === "de" ? "Noch kein führender Bereich zum Vergleichen." : "No top lane to compare yet."}
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:272:  const displayKind = locale === "de" ? (kindLabel === "tasks" ? "Aufgaben" : "Sprints") : kindLabel;
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:273:  const displaySingularKind = locale === "de" ? (kindLabel === "tasks" ? "Aufgabe" : "Sprint") : singularKind;
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:275:    last: locale === "de" ? "Neueste" : "Latest",
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:277:    active: locale === "de" ? "Aktiv" : "Active",
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:278:    input: locale === "de" ? "Eingabe" : "Input",
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:279:    output: locale === "de" ? "Ausgabe" : "Output",
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:280:    name: locale === "de" ? "Name" : "Name",
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:285:    ? (locale === "de" ? `${formatNumber(filteredItems.length)} von ${formatNumber(items.length)} ${displayKind} passend zu ${query.trim()}, sortiert nach ${sortLabelByKey[sortKey]} ${sortDir === "desc" ? "absteigend" : "aufsteigend"}.` : `Showing ${formatNumber(filteredItems.length)} of ${formatNumber(items.length)} ${kindLabel} matching ${query.trim()}, sorted by ${sortLabelByKey[sortKey]} ${sortDir === "desc" ? "descending" : "ascending"}.`)
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:286:    : (locale === "de" ? `${formatNumber(filteredItems.length)} von ${formatNumber(items.length)} ${displayKind}, sortiert nach ${sortLabelByKey[sortKey]} ${sortDir === "desc" ? "absteigend" : "aufsteigend"}.` : `Showing ${formatNumber(filteredItems.length)} of ${formatNumber(items.length)} ${kindLabel}, sorted by ${sortLabelByKey[sortKey]} ${sortDir === "desc" ? "descending" : "ascending"}.`);
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:296:              {locale === "de" ? `${displayKind} nach Aktualität, Tokens, aktiver Zeit und gerichtetem Token-Fluss durchsuchen, sortieren und vergleichen.` : `Search, sort, and compare ${kindLabel} by recency, tokens, active time, and directional token flow.`}
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:301:            {formatNumber(filteredItems.length)} {locale === "de" ? "sichtbar" : "visible"} / {formatNumber(items.length)} {locale === "de" ? "gesamt" : "total"}
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:312:              label={locale === "de" ? "Einträge" : "Entities"}
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:318:              label={locale === "de" ? "Tokens gesamt" : "Total Tokens"}
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:320:              detail={`${formatNumber(overallTotals.invocationCount)} ${locale === "de" ? "Aufrufe" : "calls"} · ${formatCost(overallTotals.totalCostUsd, locale)}`}
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:325:              label={locale === "de" ? "Ø Tokens" : "Avg Tokens"}
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:327:              detail={`${locale === "de" ? "pro" : "per"} ${displaySingularKind}`}
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:331:              label={locale === "de" ? "Ø aktiv" : "Avg Active"}
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:333:              detail={`${formatNumber(averageCalls, { minimumFractionDigits: 1, maximumFractionDigits: 1 })} ${locale === "de" ? "Aufrufe" : "calls"}/${displaySingularKind}`}
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:337:              label={locale === "de" ? "Neueste Aktivität" : "Most Recent"}
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:339:              detail={locale === "de" ? "letzte Aktivität" : "last activity"}
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:343:              label={locale === "de" ? "Führender Beitrag" : "Top Contributor"}
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:345:              detail={topItem ? `${formatTokens(topItem.usage.totalTokens, locale)} Tokens` : locale === "de" ? "keine Führung" : "no leader"}
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:351:            {locale === "de" ? `In diesem Zeitraum ist noch keine ${displayKind}-Telemetrie verfügbar.` : `No ${kindLabel} telemetry is available in this window yet.`}
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:358:              {locale === "de" ? `${displayKind} durchsuchen` : `Search ${kindLabel}`}
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:366:              placeholder={locale === "de" ? `${displayKind} durchsuchen` : `Search ${kindLabel}`}
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:373:                aria-label={locale === "de" ? "Suche leeren" : "Clear search"}
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:380:          <div className="flex max-w-full flex-wrap gap-2 pr-1 lg:justify-end" role="group" aria-label={locale === "de" ? `Sortiersteuerung für ${title}` : `${title} sort controls`}>
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:382:              ["last", locale === "de" ? "Neueste" : "Latest"],
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:384:              ["active", locale === "de" ? "Aktiv" : "Active"],
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:385:              ["input", locale === "de" ? "Eingabe" : "Input"],
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:386:              ["output", locale === "de" ? "Ausgabe" : "Output"],
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:420:                  label={locale === "de" ? "Suchergebnisse" : "Search Results"}
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:422:                  detail={queryIsActive ? (locale === "de" ? `passend zu „${query.trim()}“` : `matching "${query.trim()}"`) : locale === "de" ? "alle sichtbar" : "all visible"}
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:427:                  label={locale === "de" ? "Führender Bereich" : "Top Lane"}
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:429:                  detail={topItem ? `${formatTokens(topItem.usage.totalTokens, locale)} Tokens` : locale === "de" ? "noch kein Bereich" : "no lane yet"}
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:434:                  label={locale === "de" ? "Sichtbarer Anteil" : "Visible Share"}
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:436:                  detail={locale === "de" ? "der Tokens im aktuellen Zeitraum" : "of current window tokens"}
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:451:                const providerLabel = item.provider ? String(item.provider) : locale === "de" ? "Kein Anbieter" : "No provider";
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:452:                const purposeLabel = item.purpose ? item.purpose : locale === "de" ? "Kein Zweck" : "No purpose";
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:453:                const statusLabel = item.status ? item.status : locale === "de" ? "Kein Status" : "No status";
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:458:                  : locale === "de" ? "Keine Perzentile" : "No percentiles";
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:461:                  <div key={item.id} role="article" className={`${LEDGER_ROW_MODERN_CLASS} stats-ledger-row-joined`} aria-label={locale === "de" ? `${item.label}, Telemetriezeile für ${displaySingularKind}` : `${item.label} ${kindLabel} telemetry row`}>
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:466:                            <span className="text-[8px] tracking-[0.12em] text-[color:var(--stats-label-color)]">{locale === "de" ? "Rang" : "Rank"}</span>
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:472:                              {formatTokens(tokenPerCall, locale)}/{locale === "de" ? "Aufruf" : "call"} · {locale === "de" ? "zuletzt" : "last"} {formatDateTime(item.lastActivityAt, locale)}
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:506:                            <div className="text-[10px] font-bold uppercase tracking-[0.16em] text-[color:var(--stats-label-color)]">{locale === "de" ? "Token" : "Tokens"}</div>
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:510:                            <div className="text-[10px] font-bold uppercase tracking-[0.16em] text-[color:var(--stats-label-color)]">{locale === "de" ? "Aktiv" : "Active"}</div>
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:514:                            <div className="text-[10px] font-bold uppercase tracking-[0.16em] text-[color:var(--stats-label-color)]">{locale === "de" ? "Aufrufe" : "Calls"}</div>
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:518:                            <div className="text-[10px] font-bold uppercase tracking-[0.16em] text-[color:var(--stats-label-color)]">{locale === "de" ? "Anteil" : "Share"}</div>
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:522:                            <div className="text-[10px] font-bold uppercase tracking-[0.16em] text-[color:var(--stats-label-color)]">{locale === "de" ? "Führung" : "Leader"}</div>
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:526:                            <div className="text-[10px] font-bold uppercase tracking-[0.16em] text-[color:var(--stats-label-color)]">{locale === "de" ? "Kosten" : "Cost"}</div>
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:542:                          aria-label={locale === "de" ? `${item.label} trägt ${formatPercent(shareOfLeader, locale)} zum Token-Volumen der führenden ${displaySingularKind} bei.` : `${item.label} contributes ${formatPercent(shareOfLeader, locale)} of the leading ${singularKind} token volume.`}
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:553:                            <span><strong className="font-semibold text-[color:var(--stats-value-color)]">{locale === "de" ? "Eingabe" : "Input"}</strong> {formatTokens(item.usage.inputTokens, locale)}</span>
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:554:                            <span><strong className="font-semibold text-[color:var(--stats-value-color)]">{locale === "de" ? "Im Cache" : "Cached"}</strong> {formatTokens(item.usage.cachedInputTokens, locale)}</span>
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:555:                            <span><strong className="font-semibold text-[color:var(--stats-value-color)]">{locale === "de" ? "Ausgabe" : "Output"}</strong> {formatTokens(item.usage.outputTokens, locale)}</span>
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:556:                            <span><strong className="font-semibold text-[color:var(--stats-value-color)]">{locale === "de" ? "Schlussfolgerung" : "Reasoning"}</strong> {formatTokens(item.usage.reasoningOutputTokens, locale)}</span>
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:571:                  {locale === "de" ? "Weitere Telemetriebereiche werden geladen …" : "Loading more telemetry lanes..."}
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:579:              <div>{locale === "de" ? `Keine ${displayKind} passen zu „${query.trim()}“. ` : `No ${kindLabel} match “${query.trim()}”.`}</div>
dashboard/src/v2/pages/stats/components/TelemetryLedger.tsx:585:                {locale === "de" ? "Suche leeren" : "Clear search"}
dashboard/src/v2/pages/stats/components/AnalysisStudioSection.tsx:100:  const localizedMetadata: Record<StatsVisualMode, StudioMetadata> = locale === "de" ? {
dashboard/src/v2/pages/stats/components/AnalysisStudioSection.tsx:128:      <div className="text-base font-bold text-[color:var(--stats-value-color)]">{locale === "de" ? "Warten auf Telemetrie" : "Waiting for Telemetry"}</div>
dashboard/src/v2/pages/stats/components/AnalysisStudioSection.tsx:138:      aria-label={locale === "de" ? "Statistik-Analysebereich" : "Stats analysis panel"}
dashboard/src/v2/pages/stats/components/AnalysisStudioSection.tsx:146:          {locale === "de" ? "Analysen werden aus zwischengespeicherten Daten aktualisiert. Aktuelle Werte bleiben sichtbar, während der neueste Snapshot geladen wird." : "Updating analytics from cached data. Current values remain visible while the latest snapshot loads."}
dashboard/src/v2/pages/stats/components/TrendStudio.tsx:76:        value={stats.duration && stats.duration.sampleCount > 0 ? formatStatsDuration(stats.duration.p50Ms, locale) : locale === "de" ? "Keine Stichproben" : "No samples"}
dashboard/src/v2/pages/stats/components/TrendStudio.tsx:80:        label={locale === "de" ? "Geschwindigkeit" : "Velocity"}
dashboard/src/v2/pages/stats/components/TrendStudio.tsx:85:        label={locale === "de" ? "Erfolg" : "Success"}
dashboard/src/v2/pages/stats/components/TrendStudio.tsx:86:        value={finishedCount > 0 ? formatPercent((statusCounts!.completed / finishedCount) * 100, locale) : locale === "de" ? "Keine Läufe" : "No runs"}
dashboard/src/v2/pages/stats/components/TrendStudio.tsx:99:          <div className="text-[10px] font-bold uppercase tracking-[0.2em] text-[var(--stats-label-color)]">{locale === "de" ? "Zweckaktivität" : "Purpose Activity"}</div>
dashboard/src/v2/pages/stats/components/TrendStudio.tsx:101:            {locale === "de" ? "Token-Volumen, Aufrufzahl und aktive Zeit nach Zweck im ausgewählten Zeitraum." : "Token volume, invocation count, and active time by purpose over the selected window."}
dashboard/src/v2/pages/stats/components/TrendStudio.tsx:105:          {formatNumber(stats.purposes.length)} {locale === "de" ? "Zwecke" : "purposes"}
dashboard/src/v2/pages/stats/components/TrendStudio.tsx:122:                <span className="text-[10px] font-bold uppercase tracking-[0.12em]">{locale === "de" ? "Aufrufe" : "invocations"}</span>
dashboard/src/v2/pages/stats/components/TrendStudio.tsx:126:                <span className="text-[10px] font-bold uppercase tracking-[0.12em]">{locale === "de" ? "aktive Zeit" : "active time"}</span>
dashboard/src/v2/pages/stats/components/TrendStudio.tsx:132:            {locale === "de" ? "Für diesen Zeitraum ist keine Zweckaktivität verfügbar." : "No purpose activity is available for this range."}
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:137:      return locale === "de" ? `Aufrufe nach ${label} sortieren` : `Sort invocations by ${label}`;
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:140:    return locale === "de" ? `Aufrufe nach ${label} sortieren, aktuell ${sort.dir === "asc" ? "aufsteigend" : "absteigend"}` : `Sort invocations by ${label}, currently sorted ${sort.dir === "asc" ? "ascending" : "descending"}`;
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:159:            {locale === "de" ? "Laufend" : "Running"}
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:167:            {locale === "de" ? "Abgeschlossen" : "Completed"}
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:175:            {locale === "de" ? "Fehlgeschlagen" : "Failed"}
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:183:            {locale === "de" ? "Abgebrochen" : "Cancelled"}
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:191:            {locale === "de" ? "Pausiert" : "Paused"}
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:204:    const fallback = locale === "de" ? "unbekannt" : "unknown";
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:216:        title={locale === "de" ? "Aufrufdatensätze werden geladen" : "Loading invocation records"}
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:217:        detail={locale === "de" ? "Ledger-Zeilen und Ziele für die Protokollerweiterung werden aktualisiert." : "Refreshing the ledger rows and transcript expansion targets."}
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:219:        ariaLabel={locale === "de" ? "Aufrufdatensätze werden geladen" : "Loading invocation records"}
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:236:        title={locale === "de" ? "Aufrufdatensätze konnten nicht geladen werden" : "Failed to load invocation records"}
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:239:        ariaLabel={locale === "de" ? "Aufrufdatensätze konnten nicht geladen werden" : "Invocation records failed to load"}
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:249:        title={locale === "de" ? "Keine Aufrufdatensätze anzuzeigen" : "No invocation records to show"}
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:250:        detail={locale === "de" ? "Keine Datensätze entsprechen den aktuellen Filtern oder der Datensatzansicht." : "No records match the current filters or record view."}
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:252:        ariaLabel={locale === "de" ? "Keine Aufrufdatensätze" : "No invocation records"}
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:262:          {locale === "de" ? "Aufrufdatensätze werden aktualisiert. Zwischengespeicherte Zeilen werden angezeigt, während das aktuelle Ledger lädt." : "Updating invocation records. Showing cached rows while the latest ledger loads."}
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:266:        {locale === "de" ? `${formatNumber(Math.min(visibleCount, invocations.length))} von ${formatNumber(invocations.length)} Aufrufdatensätzen werden angezeigt.` : `Showing ${formatNumber(Math.min(visibleCount, invocations.length))} of ${formatNumber(invocations.length)} invocation records.`}
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:270:          {locale === "de" ? "Aufruf-Ledger mit sortierbaren Zeit-, Token- und Dauerspalten. Zeilen enthalten Status, Typ, Modell, Token-Zahlen, Kontext und Steuerelemente zur Protokollerweiterung." : "Invocation ledger with sortable time, token, and duration columns. Rows include status, type, model, token counts, context, and transcript expansion controls."}
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:281:                {locale === "de" ? "Zeit" : "Time"} {renderSortIcon("startedAt")}
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:285:            <th id="invocations-type" scope="col" className="pb-2">{locale === "de" ? "Typ" : "Type"}</th>
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:286:            <th id="invocations-model" scope="col" className="pb-2">{locale === "de" ? "Modell" : "Model"}</th>
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:307:            <th id="invocations-cached" scope="col" className="pb-2">{locale === "de" ? "Im Cache" : "Cached"}</th>
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:315:                {locale === "de" ? "Gesamt" : "Total"} {renderSortIcon("totalTokens")}
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:325:                {locale === "de" ? "Durchschn. Dauer" : "Avg Duration"} {renderSortIcon("durationMs")}
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:328:            <th id="invocations-context" scope="col" className="pb-2">{locale === "de" ? "Kontext" : "Context"}</th>
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:329:            <th id="invocations-expand" scope="col" className="pb-2 pr-6 text-right">{locale === "de" ? "Erweitern" : "Expand"}</th>
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:338:              : locale === "de" ? "laufend" : "running";
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:353:                    <div className={mobileLabelClass}>{locale === "de" ? "Zeit" : "Time"}</div>
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:367:                    <div className={mobileLabelClass}>{locale === "de" ? "Typ" : "Type"}</div>
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:373:                    <div className={mobileLabelClass}>{locale === "de" ? "Modell" : "Model"}</div>
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:379:                        <span className="font-bold text-[color:var(--stats-value-color)]">{invocation.provider || (locale === "de" ? "unbekannt" : "unknown")}</span>
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:394:                    <div className={mobileLabelClass}>{locale === "de" ? "Im Cache" : "Cached"}</div>
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:398:                    <div className={mobileLabelClass}>{locale === "de" ? "Gesamt" : "Total"}</div>
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:402:                    <div className={mobileLabelClass}>{locale === "de" ? "Dauer" : "Duration"}</div>
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:408:                    <div className={mobileLabelClass}>{locale === "de" ? "Kontext" : "Context"}</div>
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:426:                    <div className={mobileLabelClass}>{locale === "de" ? "Nachrichten" : "Messages"}</div>
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:432:                      aria-label={isExpanded ? (locale === "de" ? `Aufruf ${invocation.id} reduzieren` : `Collapse invocation ${invocation.id}`) : (locale === "de" ? `Aufruf ${invocation.id} erweitern` : `Expand invocation ${invocation.id}`)}
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:460:            aria-label={locale === "de" ? `Weitere Aufrufe anzeigen, aktuell ${formatNumber(Math.min(visibleCount, invocations.length))} von ${formatNumber(invocations.length)}` : `Show more invocations, currently showing ${formatNumber(Math.min(visibleCount, invocations.length))} of ${formatNumber(invocations.length)}`}
dashboard/src/v2/pages/stats/components/system/InvocationsTable.tsx:463:            {locale === "de" ? "Weitere Aufrufe anzeigen" : "Show more invocations"}
dashboard/src/v2/pages/stats/components/system/InvocationMessagesPanel.tsx:81:    return locale === "de" ? "laufend" : "running";
dashboard/src/v2/pages/stats/components/system/InvocationMessagesPanel.tsx:87:    return locale === "de" ? "laufend" : "running";
dashboard/src/v2/pages/stats/components/system/InvocationMessagesPanel.tsx:98:      return <span className={`${baseClass} ${STATUS_TONE_CLASS.signal}`}>{locale === "de" ? "Laufend" : "Running"}</span>;
dashboard/src/v2/pages/stats/components/system/InvocationMessagesPanel.tsx:100:      return <span className={`${baseClass} ${STATUS_TONE_CLASS.positive}`}>{locale === "de" ? "Abgeschlossen" : "Completed"}</span>;
dashboard/src/v2/pages/stats/components/system/InvocationMessagesPanel.tsx:102:      return <span className={`${baseClass} ${STATUS_TONE_CLASS.negative}`}>{locale === "de" ? "Fehlgeschlagen" : "Failed"}</span>;
dashboard/src/v2/pages/stats/components/system/InvocationMessagesPanel.tsx:104:      return <span className={`${baseClass} ${STATUS_TONE_CLASS.neutral}`}>{locale === "de" ? "Abgebrochen" : "Cancelled"}</span>;
dashboard/src/v2/pages/stats/components/system/InvocationMessagesPanel.tsx:106:      return <span className={`${baseClass} ${STATUS_TONE_CLASS.warning}`}>{locale === "de" ? "Pausiert" : "Paused"}</span>;
dashboard/src/v2/pages/stats/components/system/InvocationMessagesPanel.tsx:166:    labels.push(`${formatTokens(tokens, locale)} ${locale === "de" ? "Token" : "tokens"}`);
dashboard/src/v2/pages/stats/components/system/InvocationMessagesPanel.tsx:168:    labels.push(`${formatTokens(inputTokens ?? 0, locale)} ${locale === "de" ? "ein" : "in"} / ${formatTokens(outputTokens ?? 0, locale)} ${locale === "de" ? "aus" : "out"}`);
dashboard/src/v2/pages/stats/components/system/InvocationMessagesPanel.tsx:227:    { label: locale === "de" ? "Modell" : "Model", value: invocation.model || (locale === "de" ? "Unbekanntes Modell" : "Unknown model") },
dashboard/src/v2/pages/stats/components/system/InvocationMessagesPanel.tsx:228:    { label: locale === "de" ? "Dauer" : "Duration", value: formatStatsDurationLabel(invocation, locale) },
dashboard/src/v2/pages/stats/components/system/InvocationMessagesPanel.tsx:229:    { label: locale === "de" ? "Gesamt" : "Total", value: `${formatTokens(invocation.totalTokens ?? 0, locale)} ${locale === "de" ? "Token" : "tokens"}` },
dashboard/src/v2/pages/stats/components/system/InvocationMessagesPanel.tsx:230:    { label: locale === "de" ? "Fluss" : "Flow", value: `${formatTokens(invocation.inputTokens ?? 0, locale)} ${locale === "de" ? "ein" : "in"} / ${formatTokens(invocation.outputTokens ?? 0, locale)} ${locale === "de" ? "aus" : "out"}` },
dashboard/src/v2/pages/stats/components/system/InvocationMessagesPanel.tsx:232:      ? [{ label: locale === "de" ? "Im Cache" : "Cached", value: `${formatTokens(invocation.cachedInputTokens, locale)} ${locale === "de" ? "Token" : "tokens"}` }]
dashboard/src/v2/pages/stats/components/system/InvocationMessagesPanel.tsx:234:    { label: locale === "de" ? "Nachrichten" : "Messages", value: formatNumber(messageCount) },
dashboard/src/v2/pages/stats/components/system/InvocationMessagesPanel.tsx:257:      aria-label={locale === "de" ? `Nachrichtenprotokoll des Aufrufs ${invocation.id}` : `Invocation ${invocation.id} message transcript`}
dashboard/src/v2/pages/stats/components/system/InvocationMessagesPanel.tsx:264:            <span>{locale === "de" ? "Fehlerzusammenfassung" : "Error Summary"}</span>
dashboard/src/v2/pages/stats/components/system/InvocationMessagesPanel.tsx:265:            <span className="text-[9px] text-[color:var(--stats-label-color)] group-open:hidden">{locale === "de" ? "Öffnen" : "Open"}</span>
dashboard/src/v2/pages/stats/components/system/InvocationMessagesPanel.tsx:266:            <span className="hidden text-[9px] text-[color:var(--stats-label-color)] group-open:inline">{locale === "de" ? "Schließen" : "Close"}</span>
dashboard/src/v2/pages/stats/components/system/InvocationMessagesPanel.tsx:278:              {locale === "de" ? "Nachrichtenprotokoll" : "Message Transcript"}
dashboard/src/v2/pages/stats/components/system/InvocationMessagesPanel.tsx:281:              {locale === "de" ? "Letzte Nachricht" : "Last message"} {formatDateTime(invocation.lastMessageAt, locale)}
dashboard/src/v2/pages/stats/components/system/InvocationMessagesPanel.tsx:287:            aria-label={locale === "de" ? "Als JSON kopieren" : "Copy as JSON"}
dashboard/src/v2/pages/stats/components/system/InvocationMessagesPanel.tsx:310:          title={locale === "de" ? "Protokollnachrichten werden geladen" : "Loading transcript messages"}
dashboard/src/v2/pages/stats/components/system/InvocationMessagesPanel.tsx:311:          detail={locale === "de" ? "Die gespeicherte Nachrichtenliste für diesen Aufruf wird abgerufen." : "Fetching the recorded message list for this invocation."}
dashboard/src/v2/pages/stats/components/system/InvocationMessagesPanel.tsx:313:          ariaLabel={locale === "de" ? "Protokollnachrichten werden geladen" : "Loading transcript messages"}
dashboard/src/v2/pages/stats/components/system/InvocationMessagesPanel.tsx:320:          title={locale === "de" ? "Aufrufnachrichten konnten nicht geladen werden" : "Failed to load invocation messages"}
dashboard/src/v2/pages/stats/components/system/InvocationMessagesPanel.tsx:323:          ariaLabel={locale === "de" ? "Protokollnachrichten konnten nicht geladen werden" : "Transcript messages failed to load"}
dashboard/src/v2/pages/stats/components/system/InvocationMessagesPanel.tsx:329:          title={locale === "de" ? "Keine Protokollnachrichten erfasst" : "No transcript messages recorded"}
dashboard/src/v2/pages/stats/components/system/InvocationMessagesPanel.tsx:330:          detail={locale === "de" ? "Für diesen Aufruf sind keine gespeicherten Nachrichten vorhanden." : "This invocation has no stored message records to inspect."}
dashboard/src/v2/pages/stats/components/system/InvocationMessagesPanel.tsx:332:          ariaLabel={locale === "de" ? "Keine Protokollnachrichten" : "No transcript messages"}
dashboard/src/v2/pages/stats/components/system/InvocationMessagesPanel.tsx:355:                aria-label={`${message.role} ${locale === "de" ? "Nachricht" : "message"} ${formatNumber(index + 1)}`}
dashboard/src/v2/pages/stats/components/system/InvocationMessagesPanel.tsx:394:                    aria-label={locale === "de" ? `${isExpanded ? "Reduzieren" : "Erweitern"}: Systemnachricht ${formatNumber(index + 1)}` : `${isExpanded ? "Collapse" : "Expand"} system message ${formatNumber(index + 1)}`}
dashboard/src/v2/pages/stats/components/system/InvocationMessagesPanel.tsx:397:                    {isExpanded ? (locale === "de" ? "Weniger anzeigen" : "Show less") : (locale === "de" ? "Mehr anzeigen" : "Show more")}
dashboard/src/v2/pages/stats/components/system/InvocationMessagesPanel.tsx:411:              {locale === "de" ? `Alle ${formatNumber(messages.length)} Nachrichten anzeigen` : `Show all ${formatNumber(messages.length)} messages`}
dashboard/src/v2/pages/stats/components/system/InvocationMessagesPanel.tsx:418:              {locale === "de" ? "Das Protokoll wird aus Gründen der Lesbarkeit und Sicherheit als Klartext dargestellt." : "Transcript rendered as plain text for readability and safety."}
dashboard/src/v2/pages/stats/components/system/SystemFilterBar.tsx:64:      aria-label={`${label} ${locale === "de" ? "Filter" : "filters"}`}
dashboard/src/v2/pages/stats/components/system/SystemFilterBar.tsx:101:            {locale === "de" ? "Suche" : "Search"}
dashboard/src/v2/pages/stats/components/system/SystemFilterBar.tsx:110:              placeholder={locale === "de" ? "Systemstatistiken durchsuchen" : "Search system stats"}
dashboard/src/v2/pages/stats/components/system/SystemFilterBar.tsx:111:              aria-label={locale === "de" ? "Systemstatistiken durchsuchen" : "Search system stats"}
dashboard/src/v2/pages/stats/components/system/SystemFilterBar.tsx:118:                aria-label={locale === "de" ? "Suche leeren" : "Clear search"}
dashboard/src/v2/pages/stats/components/system/SystemFilterBar.tsx:138:                <span className="min-w-0 max-w-full truncate">{locale === "de" ? ({ running: "Laufend", completed: "Abgeschlossen", failed: "Fehlgeschlagen", cancelled: "Abgebrochen", paused: "Pausiert" } as const)[status.value] : status.label}</span>
dashboard/src/v2/pages/stats/components/system/SystemFilterBar.tsx:147:          <FilterGroup label={locale === "de" ? "Zwecke" : "Purposes"}>
dashboard/src/v2/pages/stats/components/system/SystemFilterBar.tsx:166:          <FilterGroup label={locale === "de" ? "Provider" : "Providers"}>
dashboard/src/v2/pages/stats/components/system/SystemFilterBar.tsx:184:        <FilterGroup label={locale === "de" ? "Fehlerkategorie" : "Error Category"}>
dashboard/src/v2/pages/stats/components/system/SystemFilterBar.tsx:195:                <span className="min-w-0 max-w-full truncate">{locale === "de" ? ({ timeout: "Zeitüberschreitung", rateLimit: "Ratenlimit", apiError: "API-Fehler", modelError: "Modellfehler", cancelled: "Abgebrochen" } as Record<string, string>)[errorCat] : formatChipLabel(errorCat)}</span>
dashboard/src/v2/pages/stats/components/system/SystemFilterBar.tsx:213:              {locale === "de" ? "Alle leeren" : "Clear all"}
dashboard/src/v2/pages/stats/components/system/SystemFilterBar.tsx:217:            {locale === "de" ? `${shownCount} von ${totalShown} angezeigt` : `Showing ${shownCount} of ${totalShown}`}
dashboard/src/v2/pages/stats/components/system/SystemFilterBar.tsx:220:            {formatNumber(activeFilterCount)} {locale === "de" ? "aktive Filter" : "active filters"}
dashboard/src/v2/pages/stats/components/system/SystemFilterBar.tsx:224:              {locale === "de" ? `Seite ${formatNumber(page + 1)}${hasMore ? " · weitere verfügbar" : ""}` : `Page ${formatNumber(page + 1)}${hasMore ? " · more available" : ""}`}
dashboard/src/v2/pages/stats/components/system/SystemFilterBar.tsx:230:          <div className="grid min-w-0 grid-cols-2 gap-2 sm:flex sm:flex-wrap sm:items-center lg:justify-end" role="group" aria-label={locale === "de" ? "Aufrufseitennavigation" : "Invocation pagination"}>
dashboard/src/v2/pages/stats/components/system/SystemFilterBar.tsx:238:              <span className="truncate">{locale === "de" ? "Zurück" : "Prev"}</span>
dashboard/src/v2/pages/stats/components/system/SystemFilterBar.tsx:246:              <span className="truncate">{locale === "de" ? "Weiter" : "Next"}</span>
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:83:    <div className={`${SUBPANEL_CLASS} p-4`} aria-label={locale === "de" ? "Statusverteilung" : "Status distribution"}>
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:85:        <div className="text-[10px] font-bold uppercase tracking-[0.18em] text-[color:var(--stats-label-color)]">{locale === "de" ? "Statusverteilung" : "Status Distribution"}</div>
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:90:              <span>{locale === "de" ? ({ completedCount: "Abgeschlossen", runningCount: "Laufend", failedCount: "Fehlgeschlagen", cancelledCount: "Abgebrochen", pausedCount: "Pausiert" } as const)[segment.key] : segment.label}</span>
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:105:              title={`${locale === "de" ? ({ completedCount: "Abgeschlossen", runningCount: "Laufend", failedCount: "Fehlgeschlagen", cancelledCount: "Abgebrochen", pausedCount: "Pausiert" } as const)[segment.key] : segment.label}: ${formatNumber(count)}`}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:327:          eyebrow={locale === "de" ? "Systemtelemetrie" : "System Telemetry"}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:328:          title={locale === "de" ? "Systembetrieb" : "System Operations"}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:329:          description={locale === "de" ? "Projekt-Aufrufarbeitsbereich für Sprintstatus, Aufrufzustand, externe API-Aktivität, Filter, Seitennavigation und erweiterbare Nachrichtendetails." : "Project invocation workbench for sprint state, invocation health, external API activity, filters, pagination, and expandable message detail."}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:334:        eyebrow={locale === "de" ? "Sprintübersicht" : "Sprint Overview"}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:335:        title={locale === "de" ? "Sprintstatus" : "Sprint State"}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:336:        description={locale === "de" ? "Der aktuelle Sprint- und Aufgabenstatus bleibt von Aufrufmetriken getrennt, damit aktive, blockierte und abgeschlossene Arbeit zuerst erfasst werden kann." : "Current sprint and task state stays separate from invocation metrics so active work, blocked work, and settled work can be scanned first."}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:337:        action={<SectionCount label={locale === "de" ? "Aufgaben" : "Tasks"} value={sprintData.totalTasks} />}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:344:            detail={locale === "de" ? "über Protokolle erfasst" : "recorded across logs"}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:349:            label={locale === "de" ? "Aktiv" : "Active"}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:351:            detail={sprintData.activeSprints > 0 ? `${formatNumber(sprintData.runningTasks)} ${locale === "de" ? "Aufgaben aktiv" : "tasks live"}` : locale === "de" ? "alles abgeschlossen" : "all settled"}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:357:            label={locale === "de" ? "Abgeschlossen" : "Completed"}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:359:            detail={`${formatNumber(sprintData.failedSprints)} ${locale === "de" ? "fehlgeschlagen" : "failed"}`}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:364:            label={locale === "de" ? "Aufgaben" : "Tasks"}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:366:            detail={`${formatNumber(sprintData.blockedTasks)} ${locale === "de" ? "blockiert" : "blocked"}`}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:371:            label={locale === "de" ? "Aufrufe" : "Invocations"}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:373:            detail={`${formatNumber(summaryMetrics.runningCount)} ${locale === "de" ? "laufend" : "running"}`}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:380:        eyebrow={locale === "de" ? "Aufrufzustand" : "Invocation Health"}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:381:        title={locale === "de" ? "Zustandsübersicht" : "Health Snapshot"}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:382:        description={locale === "de" ? "Aktuelles Volumen, Erfolgsrate, Latenz, Cache-Effizienz und laufende Arbeit werden aus der serverseitigen Zusammenfassung für den aktuellen Filtersatz abgeleitet." : "Current volume, success rate, latency, cache efficiency, and in-flight work are derived from the server-projected summary for the current filter set."}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:383:        action={<SectionCount label={locale === "de" ? "Gefiltert" : "Filtered"} value={summaryMetrics.totalInvocations} />}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:389:              title={locale === "de" ? "Aufrufzustand benötigt Datensätze" : "Invocation health needs records"}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:390:              detail={locale === "de" ? "Keine Aufrufdatensätze entsprechen dem aktuellen Filtersatz; Raten- und Latenzmetriken sind daher nicht verfügbar." : "No invocation records match the current filter set, so rate and latency metrics are unavailable."}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:392:              ariaLabel={locale === "de" ? "Aufrufzustand mit reduzierten Daten" : "Invocation health reduced data"}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:399:            label={locale === "de" ? "Aufrufe" : "Invocations"}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:401:            detail={`${formatNumber(summaryMetrics.completedCount)} ${locale === "de" ? "abgeschlossen" : "completed"}`}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:406:            label={locale === "de" ? "Token gesamt" : "Total Tokens"}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:408:            detail={`${formatTokens(summaryMetrics.totalOutputTokens, locale)} ${locale === "de" ? "Ausgabe" : "output"}`}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:413:            label={locale === "de" ? "Erfolgsrate" : "Success Rate"}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:415:            detail={summaryMetrics.failedCount > 0 ? `${formatNumber(summaryMetrics.failedCount)} ${locale === "de" ? "fehlgeschlagen" : "failed"}` : summaryMetrics.totalInvocations > 0 ? (locale === "de" ? "keine Fehler in den Daten" : "no failures in data") : (locale === "de" ? "benötigt Datensätze" : "needs records")}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:421:            label={locale === "de" ? "Durchschn. Dauer" : "Avg Duration"}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:423:            detail={summaryMetrics.p95DurationMs > 0 ? `p95 ${formatStatsDuration(summaryMetrics.p95DurationMs, locale)}` : locale === "de" ? "keine beendeten Aufrufe" : "no finished calls"}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:428:            label={locale === "de" ? "Cache-Treffer" : "Cache Hits"}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:430:            detail={`${formatTokens(summaryMetrics.totalCachedTokens, locale)} ${locale === "de" ? "Token im Cache" : "cached tokens"}`}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:435:            label={locale === "de" ? "Laufend" : "Running"}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:437:            detail={summaryMetrics.runningCount > 0 ? (locale === "de" ? "gerade aktiv" : "live right now") : (locale === "de" ? "alles abgeschlossen" : "all settled")}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:448:        eyebrow={locale === "de" ? "Externe APIs" : "External APIs"}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:449:        title={locale === "de" ? "Externe API-Aktivität" : "External API Activity"}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:450:        description={locale === "de" ? "Git, Jules, Jira und andere Integrationen sind von der Hauptaufruftabelle getrennt, damit externer Datenverkehr leicht geprüft werden kann." : "Git, Jules, Jira, and other integrations are isolated from the main invocation table so external traffic stays easy to audit."}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:451:        action={<SectionCount label={locale === "de" ? "Aufrufe" : "Calls"} value={externalApiCallCount} />}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:457:              title={locale === "de" ? "Keine externe API-Aktivität klassifiziert" : "No external API activity classified"}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:458:              detail={locale === "de" ? "Dieser Datensatz enthält keine klassifizierten Git-, Jules-, Jira- oder anderen externen API-Aufrufe." : "This data set does not include classified Git, Jules, Jira, or other external API calls."}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:460:              ariaLabel={locale === "de" ? "Keine externe API-Aktivität" : "No external API activity"}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:471:              detail={metrics.calls > 0 ? formatStatsDuration(metrics.avgDurationMs, locale) : locale === "de" ? "Keine klassifizierten Aufrufe" : "No classified calls"}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:479:        eyebrow={locale === "de" ? "Fehleranalyse" : "Failure Analysis"}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:480:        title={locale === "de" ? "Fehlerkategorien" : "Error Categories"}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:481:        description={locale === "de" ? "Fehlerklassen werden gruppiert, damit vorübergehende Probleme von Provider-, Modell- und Abbruchproblemen unterschieden werden können." : "Error classes are grouped so operators can separate transient issues from provider, model, and cancellation problems at a glance."}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:482:        action={totalErrors > 0 ? <SectionCount label={locale === "de" ? "Fehler" : "Failures"} value={totalErrors} tone="warning" /> : undefined}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:488:              title={locale === "de" ? "Keine Fehlerkategorien klassifiziert" : "No error categories classified"}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:489:              detail={locale === "de" ? "Fehlgeschlagene oder abgebrochene Aufrufe mit klassifizierbaren Details erscheinen hier." : "Failed or cancelled invocations with classifiable details will appear here."}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:491:              ariaLabel={locale === "de" ? "Keine Fehlerkategorien" : "No error categories"}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:497:                const label = locale === "de"
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:517:        eyebrow={locale === "de" ? "Aufrufdatensätze" : "Invocation Records"}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:518:        title={locale === "de" ? "Aufrufdatensätze" : "Invocation Records"}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:519:        description={locale === "de" ? "Suche, Serverfilter, Datensatzregister, Ergebniszahlen, Seitennavigation und erweiterbare Protokolldetails bleiben in einem operativen Datensatzbereich." : "Search, server filters, record tabs, result counts, pagination, and expandable transcript detail stay in one operational record area."}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:523:            aria-label={locale === "de" ? `Verfügbare Aufrufdatensätze: ${formatNumber(invocations.length)}` : `Available invocation records: ${formatNumber(invocations.length)}`}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:525:            <span>{locale === "de" ? "Verfügbar" : "Available"}</span>
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:535:              <div className="text-[10px] font-bold uppercase tracking-[0.16em] text-[color:var(--stats-label-color)]">{locale === "de" ? "Datensatzansichten" : "Record Views"}</div>
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:539:                aria-label={locale === "de" ? "Ansichten der Aufrufdatensätze" : "Invocation record views"}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:544:                  const tabLabel = tab === "all" ? (locale === "de" ? "Alle" : "All") : tab === "errors" ? (locale === "de" ? "Fehler" : "Errors") : (locale === "de" ? "Systemnachr." : "System Msgs");
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:554:                      aria-label={locale === "de" ? `${tabLabel}, ${formatNumber(tabCount)} ${tabCount === 1 ? "Datensatz" : "Datensätze"}` : `${tabLabel} invocation records, ${formatNumber(tabCount)} ${tabCount === 1 ? "record" : "records"}`}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:575:              <span>{locale === "de" ? "Filter" : "Filters"}</span>
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:596:              title={locale === "de" ? "Aufrufe konnten nicht geladen werden" : "Failed to load invocations"}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:599:              ariaLabel={locale === "de" ? "Laden der Aufrufe fehlgeschlagen" : "Invocation load failed"}
dashboard/src/v2/pages/stats/components/system/SystemStudio.tsx:606:              <span>{locale === "de" ? "Aufruf-Ledger" : "Invocation Ledger"}</span>
dashboard/src/v2/pages/stats/components/TelemetrySectionsSection.tsx:23:          <span className={CHIP_CLASS}>{formatNumber(stats.purposes.length)} {locale === "de" ? "Zwecktypen" : "Purpose Types"}</span>
dashboard/src/v2/pages/stats/components/TelemetrySectionsSection.tsx:24:          <span className={CHIP_CLASS}>{formatStatsDuration(stats.usage.activeTimeMs, locale)} {locale === "de" ? "aktive Zeit" : "Active Time"}</span>
dashboard/src/v2/pages/stats/components/TelemetrySectionsSection.tsx:28:          eyebrow={locale === "de" ? "Telemetrieprotokolle" : "Telemetry Ledgers"}
dashboard/src/v2/pages/stats/components/TelemetrySectionsSection.tsx:29:          title={locale === "de" ? "Aufgaben- und Sprint-Telemetrie" : "Task and sprint telemetry"}
dashboard/src/v2/pages/stats/components/TelemetrySectionsSection.tsx:30:          description={locale === "de" ? `Detaillierte operative Protokolle für Ausführungsbereiche mit Suche, Aktualität, Sortierung und umfangreicheren Nutzungsaufschlüsselungen. — ${stats.range.label}` : `Deep operational ledgers for execution scopes, redesigned around search, recency, sort controls, and richer usage breakdowns. — ${stats.range.label}`}
dashboard/src/v2/pages/stats/components/UsageChartMinimap.tsx:48:    const startLabel = buckets[start]?.label ?? `${locale === "de" ? "Intervall" : "bucket"} ${formatNumber(start + 1)}`;
dashboard/src/v2/pages/stats/components/UsageChartMinimap.tsx:49:    const endLabel = buckets[end]?.label ?? `${locale === "de" ? "Intervall" : "bucket"} ${formatNumber(end + 1)}`;
dashboard/src/v2/pages/stats/components/UsageChartMinimap.tsx:50:    return locale === "de" ? `Übersicht auf ${startLabel} bis ${endLabel} gezoomt, ${formatNumber(end - start + 1)} von ${formatNumber(buckets.length)} Intervallen.` : `Zoomed overview to ${startLabel} through ${endLabel}, ${formatNumber(end - start + 1)} of ${formatNumber(buckets.length)} buckets.`;
dashboard/src/v2/pages/stats/components/UsageChartMinimap.tsx:100:      onStatusChange?.(locale === "de" ? `Zoom auf den gesamten Zeitraum mit ${formatNumber(buckets.length)} Intervallen zurückgesetzt.` : `Zoom reset to the full ${formatNumber(buckets.length)}-bucket range.`);
dashboard/src/v2/pages/stats/components/UsageChartMinimap.tsx:109:        onStatusChange?.(locale === "de" ? 'Ansicht mit einem Intervall. Zoom ist erst mit weiteren Intervallen verfügbar.' : 'Single-bucket view. Zoom is unavailable until more buckets exist.');
dashboard/src/v2/pages/stats/components/UsageChartMinimap.tsx:117:      onStatusChange?.(locale === "de" ? `Zoom auf den gesamten Zeitraum mit ${formatNumber(buckets.length)} Intervallen zurückgesetzt.` : `Zoom reset to the full ${formatNumber(buckets.length)}-bucket range.`);
dashboard/src/v2/pages/stats/components/UsageChartMinimap.tsx:125:        onStatusChange?.(locale === "de" ? `Übersicht auf den gesamten Zeitraum mit ${formatNumber(buckets.length)} Intervallen fixiert.` : `Pinned overview to the full ${formatNumber(buckets.length)}-bucket range.`);
dashboard/src/v2/pages/stats/components/UsageChartMinimap.tsx:158:          {locale === "de" ? "Übersicht – zum Zoomen ziehen, mit Pfeiltasten verschieben, mit Escape zurücksetzen" : "Overview - drag to zoom, arrow keys to pan, escape to reset"}
dashboard/src/v2/pages/stats/components/UsageChartMinimap.tsx:162:            {formatNumber(zoomRange.end - zoomRange.start + 1)} {locale === "de" ? "von" : "of"} {formatNumber(buckets.length)} {locale === "de" ? "Intervallen" : "buckets"}
dashboard/src/v2/pages/stats/components/UsageChartMinimap.tsx:166:            {locale === "de" ? "Ansicht mit einem Intervall" : "Single-bucket view"}
dashboard/src/v2/pages/stats/components/UsageChartMinimap.tsx:175:          ? (locale === "de" ? `Zoomregion der Diagramm-Minimap, Intervalle ${formatNumber(zoomRange.start + 1)} bis ${formatNumber(zoomRange.end + 1)} von ${formatNumber(buckets.length)}` : `Chart minimap zoom region, showing buckets ${formatNumber(zoomRange.start + 1)} through ${formatNumber(zoomRange.end + 1)} of ${formatNumber(buckets.length)}`)
dashboard/src/v2/pages/stats/components/UsageChartMinimap.tsx:176:          : (locale === "de" ? `Zoomregion der Diagramm-Minimap, gesamter Zeitraum mit ${formatNumber(buckets.length)} Intervallen` : `Chart minimap zoom region, full range of ${formatNumber(buckets.length)} bucket${buckets.length === 1 ? '' : 's'}`)}
dashboard/src/v2/pages/stats/components/UsageChartMinimap.tsx:188:            ? (locale === "de" ? `${formatNumber(zoomRange.end - zoomRange.start + 1)} von ${formatNumber(buckets.length)} Intervallen, ${buckets[zoomRange.start]?.label ?? "Start"} bis ${buckets[zoomRange.end]?.label ?? "Ende"}.` : `Showing ${formatNumber(zoomRange.end - zoomRange.start + 1)} of ${formatNumber(buckets.length)} buckets, ${buckets[zoomRange.start]?.label ?? "start"} through ${buckets[zoomRange.end]?.label ?? "end"}.`)
dashboard/src/v2/pages/stats/components/UsageChartMinimap.tsx:190:              ? (locale === "de" ? `Gesamter Zeitraum mit ${formatNumber(buckets.length)} Intervallen angezeigt. Zum Zoomen ziehen oder die Eingabetaste drücken, um den Zeitraum zu fixieren.` : `Full ${formatNumber(buckets.length)}-bucket range shown. Drag to zoom or press Enter to pin the full range.`)
dashboard/src/v2/pages/stats/components/UsageChartMinimap.tsx:191:              : (locale === "de" ? 'Ansicht mit einem Intervall. Zoom ist erst mit weiteren Intervallen verfügbar.' : 'Single-bucket view. Zoom is unavailable until more buckets exist.')}
dashboard/src/v2/pages/stats/components/UsageChartMinimap.tsx:194:          {locale === "de" ? "Intervallreihenfolge der Minimap" : "Minimap bucket order"}: {buckets.map((bucket, index) => `${formatNumber(index + 1)}. ${bucket.label}, ${formatNumber(bucket.usage.totalTokens)} Tokens`).join("; ")}.
dashboard/src/v2/pages/stats/components/UsageChartMinimap.tsx:244:            {locale === "de" ? "Zoom wird verfügbar, sobald das nächste Intervall eintrifft." : "Zoom becomes available after the next bucket lands."}
dashboard/src/v2/pages/stats/components/UsageSeriesSidebar.tsx:15:    <div className="grid gap-2" role="list" aria-label={locale === "de" ? "Aktuelle Werte aktiver Reihen" : "Current values for active series"}>
dashboard/src/v2/pages/stats/components/UsageSeriesSidebar.tsx:23:            aria-label={`${s.label}: ${s.formatter(currentValue)}, ${s.signalLabel || (locale === "de" ? 'Kennzahl' : 'Metric')}`}
dashboard/src/v2/pages/stats/components/UsageSeriesSidebar.tsx:32:              <div className="text-right text-[10px] font-bold uppercase tracking-[0.14em] text-[var(--stats-detail-color)] opacity-80">{s.signalLabel || (locale === "de" ? 'Kennzahl' : 'Metric')}</div>
dashboard/src/v2/pages/stats/components/UsageSeriesSidebar.tsx:40:      {locale === "de" ? "Mindestens eine Reihe muss aktiviert bleiben, um aktuelle Intervallwerte zu prüfen." : "Keep at least one series enabled to inspect live bucket values."}
dashboard/src/v2/pages/stats/components/TelemetryLedgerTabs.tsx:28:    return `${new Intl.NumberFormat(locale, { minimumFractionDigits: 1, maximumFractionDigits: 1 }).format(value / 1_000_000)}${locale === "de" ? " Mio." : "M"}`;
dashboard/src/v2/pages/stats/components/TelemetryLedgerTabs.tsx:57:      { id: "tasks" as const, label: locale === "de" ? "Aufgabentelemetrie" : "Task Telemetry", detail: locale === "de" ? "Anbieter-Arbeitsbereiche" : "Provider work lanes", icon: ListTodo, count: taskCount },
dashboard/src/v2/pages/stats/components/TelemetryLedgerTabs.tsx:58:      { id: "sprints" as const, label: locale === "de" ? "Sprint-Telemetrie" : "Sprint Telemetry", detail: locale === "de" ? "Sprint-Zusammenfassungen" : "Sprint rollups", icon: Rows3, count: sprintCount },
dashboard/src/v2/pages/stats/components/TelemetryLedgerTabs.tsx:59:      ...(gitStats ? [{ id: "git" as const, label: locale === "de" ? "Git-Telemetrie" : "Git Telemetry", detail: locale === "de" ? "PR- und Änderungsbereiche" : "PR and churn lanes", icon: GitBranch, count: gitCount }] : []),
dashboard/src/v2/pages/stats/components/TelemetryLedgerTabs.tsx:93:          ? (locale === "de" ? `${activeTabDetails.label} ausgewählt, ${formatNumber(activeTabDetails.count)} ${activeTabDetails.count === 1 ? "Eintrag" : "Einträge"}.` : `${activeTabDetails.label} selected, ${formatNumber(activeTabDetails.count)} ${activeTabDetails.count === 1 ? "entry" : "entries"}.`)
dashboard/src/v2/pages/stats/components/TelemetryLedgerTabs.tsx:94:          : locale === "de" ? "Telemetrieprotokoll ausgewählt." : "Telemetry ledger selected."}
dashboard/src/v2/pages/stats/components/TelemetryLedgerTabs.tsx:99:        aria-label={locale === "de" ? "Telemetrieprotokolle" : "Telemetry ledgers"}
dashboard/src/v2/pages/stats/components/TelemetryLedgerTabs.tsx:142:              aria-label={locale === "de" ? `${tab.label}, ${formatNumber(tab.count)} ${tab.count === 1 ? "Eintrag" : "Einträge"}` : `${tab.label}, ${formatNumber(tab.count)} ${tab.count === 1 ? "entry" : "entries"}`}
dashboard/src/v2/pages/stats/components/TelemetryLedgerTabs.tsx:174:            title={locale === "de" ? "Sprint-Telemetrie" : "Sprint Telemetry"}
dashboard/src/v2/pages/stats/components/TelemetryLedgerTabs.tsx:175:            eyebrow={locale === "de" ? "Sprint-Protokoll" : "Sprint Ledger"}
dashboard/src/v2/pages/stats/components/TelemetryLedgerTabs.tsx:178:            emptyLabel={locale === "de" ? "Keine aktive Sprint-Telemetrie in diesem Zeitraum." : "No sprint telemetry active in this window."}
dashboard/src/v2/pages/stats/components/TelemetryLedgerTabs.tsx:182:            title={locale === "de" ? "Aufgabentelemetrie" : "Task Telemetry"}
dashboard/src/v2/pages/stats/components/TelemetryLedgerTabs.tsx:183:            eyebrow={locale === "de" ? "Aufgabenprotokoll" : "Task Ledger"}
dashboard/src/v2/pages/stats/components/TelemetryLedgerTabs.tsx:186:            emptyLabel={locale === "de" ? "Noch keine Aufgabentelemetrie in diesem Zeitraum." : "No task telemetry landed in this window yet."}
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:71:        <div className={styles.tokenLaneShare}>{share === null ? (locale === "de" ? "Kein Volumen" : "No volume") : `${formatPercent(share, locale)} ${locale === "de" ? "des Gesamtwerts" : "of total"}`}</div>
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:114:          {formatNumber(purpose.usage.invocationCount)} {locale === "de" ? "Aufrufe" : "calls"} · {formatStatsDuration(purpose.usage.activeTimeMs, locale)} {locale === "de" ? "aktiv" : "active"}
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:119:        <span>{share === null ? (locale === "de" ? "Kein Anteil" : "No share") : `${formatPercent(share, locale)} ${locale === "de" ? "Anteil" : "share"}`}</span>
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:149:    <section className={styles.studio} aria-label={locale === "de" ? "Aufschlüsselung der Zusammensetzung" : "Composition breakdown"}>
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:154:            eyebrow={locale === "de" ? "Token-Struktur" : "Token anatomy"}
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:155:            title={locale === "de" ? "Wie der Zeitraum genutzt wurde" : "How the window was consumed"}
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:156:            description={locale === "de" ? "Direkte Übersicht über Prompt-, Cache-, Generierungs- und Schlussfolgerungsvolumen." : "A direct read of prompt, cache, generation, and reasoning volume."}
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:157:            meta={`${formatTokens(stats.usage.totalTokens, locale)} ${locale === "de" ? "gesamt" : "total"}`}
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:162:              <div className={styles.dataLabel}>{locale === "de" ? "Token-Volumen gesamt" : "Total token volume"}</div>
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:165:            <div className={styles.flowCaption}>{locale === "de" ? "Eingabe → Cache → Ausgabe → Schlussfolgerung" : "Input → cache → output → reasoning"}</div>
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:176:            <TokenLane icon={ArrowDownRight} label={locale === "de" ? "Eingabe" : "Input"} value={stats.usage.inputTokens} total={stats.usage.totalTokens} tone="input" />
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:177:            <TokenLane icon={Database} label={locale === "de" ? "Cache-Eingabe" : "Cached input"} value={stats.usage.cachedInputTokens} total={stats.usage.totalTokens} tone="cached" />
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:178:            <TokenLane icon={ArrowUpRight} label={locale === "de" ? "Ausgabe" : "Output"} value={stats.usage.outputTokens} total={stats.usage.totalTokens} tone="output" />
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:179:            <TokenLane icon={Brain} label={locale === "de" ? "Schlussfolgerung" : "Reasoning"} value={stats.usage.reasoningOutputTokens} total={stats.usage.totalTokens} tone="reasoning" />
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:184:              <div className={styles.dataLabel}>{locale === "de" ? "Cache-Effizienz" : "Cache efficiency"}</div>
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:189:                ? (locale === "de" ? "In diesem Zeitraum wurde keine cachefähige Eingabe erfasst." : "No cacheable input was recorded in this window.")
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:190:                : (locale === "de" ? `${formatTokens(stats.usage.cachedInputTokens, locale)} Tokens wurden aus dem Cache bereitgestellt.` : `${formatTokens(stats.usage.cachedInputTokens, locale)} tokens were served from cache.`)}
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:198:            eyebrow={locale === "de" ? "Anbieteranteil" : "Provider share"}
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:199:            title={locale === "de" ? "Wo die Nutzung anfiel" : "Where usage landed"}
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:200:            description={locale === "de" ? "Anbieter nach ihrem Beitrag zum gesamten Token-Volumen sortiert." : "Providers ranked by their contribution to total token volume."}
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:201:            meta={`${formatNumber(segments.length)} ${locale === "de" ? (segments.length === 1 ? "Anbieter" : "Anbieter") : (segments.length === 1 ? "provider" : "providers")}`}
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:205:            <div className={DASHED_EMPTY_CLASS}>{locale === "de" ? "Keine Anbieterdaten für diesen Zeitraum." : "No provider data for this window."}</div>
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:248:            eyebrow={locale === "de" ? "Zweckbereiche" : "Purpose lanes"}
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:249:            title={locale === "de" ? "Wofür Tokens ausgegeben wurden" : "Why tokens were spent"}
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:250:            description={locale === "de" ? "Absichten nach Token-Volumen, ergänzt um Aufruf- und Aktivzeitkontext." : "Intent ranked by token volume, with invocation and active-time context."}
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:251:            meta={purposes.length > 0 ? `${formatNumber(purposes.length)} ${locale === "de" ? "erfasst" : "tracked"}` : undefined}
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:254:            <div className={DASHED_EMPTY_CLASS}>{locale === "de" ? "Keine Zweckdaten für diesen Zeitraum." : "No purpose data for this window."}</div>
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:267:            eyebrow={locale === "de" ? "Laufzeitkontext" : "Runtime context"}
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:268:            title={locale === "de" ? "Effizienz auf einen Blick" : "Efficiency at a glance"}
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:269:            description={locale === "de" ? "Zeit- und Ausgabensignale zur Einordnung der obigen Zusammensetzung." : "Time and spend signals that explain the composition above."}
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:274:              label={locale === "de" ? "Aktive Zeit" : "Active time"}
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:276:              detail={locale === "de" ? "Anbieterausführung" : "Provider execution"}
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:280:              label={locale === "de" ? "Gesamtzeit" : "Wall time"}
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:282:              detail={activeUtilization === null ? (locale === "de" ? "Nicht erfasst" : "Not tracked") : `${formatPercent(activeUtilization, locale)} ${locale === "de" ? "genutzt" : "utilized"}`}
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:287:                label={locale === "de" ? "Gesamtkosten" : "Total cost"}
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:289:                detail={locale === "de" ? "Bepreister Snapshot" : "Priced snapshot"}
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:295:                label={locale === "de" ? "Merge-Konflikte" : "Merge Conflicts"}
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:297:                detail={`${formatNumber(gitTotals.prCount)} PRs · ${formatNumber(gitTotals.mergedCount)} ${locale === "de" ? "zusammengeführt" : "merged"}`}
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:307:          eyebrow={locale === "de" ? "Anbieteraktivität" : "Provider activity"}
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:308:          title={locale === "de" ? "Anbieterdetails" : "Provider detail"}
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:309:          description={locale === "de" ? "Token-Volumen, Aufrufe, Cache-Verhalten und Laufzeiteffizienz innerhalb der Zusammensetzungsansicht." : "Token volume, calls, cache behavior, and runtime efficiency without leaving the composition lens."}
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:310:          meta={`${formatNumber(providers.length)} ${locale === "de" ? (providers.length === 1 ? "Zeile" : "Zeilen") : (providers.length === 1 ? "row" : "rows")}`}
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:314:          <div className={DASHED_EMPTY_CLASS}>{locale === "de" ? "Keine Anbieterdaten für diesen Zeitraum." : "No provider data for this window."}</div>
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:318:              <span>{locale === "de" ? "Anbieter" : "Provider"}</span>
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:319:              <span>{locale === "de" ? "Token-Verteilung" : "Token distribution"}</span>
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:320:              <span>{locale === "de" ? "Aufrufe" : "Calls"}</span>
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:322:              <span>{locale === "de" ? "Tokens/Aufruf" : "Tokens / call"}</span>
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:323:              <span>{locale === "de" ? "Aktiv" : "Active"}</span>
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:324:              <span>{locale === "de" ? "Kosten" : "Cost"}</span>
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:345:                      <p>{provider.secondaryLabel ?? (locale === "de" ? "Anbietertelemetrie" : "Provider telemetry")}</p>
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:352:                      <span>{providerShare === null ? "—" : `${formatPercent(providerShare, locale)} ${locale === "de" ? "Anteil" : "share"}`}</span>
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:364:                    <div><dt>{locale === "de" ? "Aufrufe" : "Calls"}</dt><dd>{formatNumber(provider.usage.invocationCount)}</dd></div>
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:366:                    <div><dt>{locale === "de" ? "Tokens/Aufruf" : "Tokens / call"}</dt><dd>{providerTokensPerCall === null ? "—" : formatTokens(providerTokensPerCall, locale)}</dd></div>
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:367:                    <div><dt>{locale === "de" ? "Aktiv" : "Active"}</dt><dd>{formatStatsDuration(provider.usage.activeTimeMs, locale)}</dd></div>
dashboard/src/v2/pages/stats/components/CompositionStudio.tsx:368:                    <div><dt>{locale === "de" ? "Kosten" : "Cost"}</dt><dd>{provider.usage.totalCostUsd > 0 ? formatCost(provider.usage.totalCostUsd, locale) : "—"}</dd></div>
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:68:  value > 0 ? `${formatPercent(value, locale, 1)} ${locale === "de" ? "Anteil" : "share"}` : locale === "de" ? "kein Token-Anteil" : "no token share"
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:73:    return locale === "de" ? "Noch keine Aufrufe" : "No calls yet";
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:76:    return locale === "de" ? "Kleine Stichprobe" : "Low sample";
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:78:  return `${new Intl.NumberFormat(locale).format(count)} ${locale === "de" ? "Aufrufe" : "calls"}`;
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:100:      {highlight ? highlight.value : locale === "de" ? "Noch nicht genügend Telemetrie" : "Not enough telemetry yet"}
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:137:  const statusSummary = locale === "de"
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:142:    <article className={`${PANEL_CLASS} p-4`} aria-label={locale === "de" ? `${model.label}, Rang ${formatNumber(rank)} der Modellrangliste` : `${model.label} model leaderboard rank ${formatNumber(rank)}`}>
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:168:            {locale === "de" ? "Volumenrang" : "Volume rank"}
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:182:            <span className="text-[color:var(--stats-label-color)]">{locale === "de" ? "Kosten" : "cost"}</span>
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:193:          {hasLowTelemetry ? (locale === "de" ? "Die Ranglistenplatzierung basiert auf begrenzter Aufruftelemetrie." : "Leaderboard placement is based on limited invocation telemetry.") : (locale === "de" ? "Latenzperzentile erscheinen, sobald dieses Modell Laufzeitstichproben erfasst." : "Latency percentiles will appear after this model records duration samples.")}
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:199:          label={locale === "de" ? "Aufrufe" : "Invocations"}
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:201:          detail={model.statusCounts.failed > 0 ? `${formatNumber(model.statusCounts.failed)} ${locale === "de" ? "fehlgeschlagen" : "failed"}` : locale === "de" ? "keine Fehler" : "no failures"}
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:204:          label={locale === "de" ? "Latenz" : "Latency"}
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:206:          detail={hasDuration ? `p50 · p95 ${formatStatsDuration(model.duration.p95Ms, locale)}` : locale === "de" ? "keine Laufzeitstichproben" : "no duration samples"}
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:209:          label={locale === "de" ? "Tokens/Aufruf" : "Tokens / Call"}
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:211:          detail={model.usage.invocationCount > 0 ? (locale === "de" ? "Durchschnittsvolumen" : "avg volume") : locale === "de" ? "keine Aufrufe" : "no calls"}
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:214:          label={locale === "de" ? "Erfolg" : "Success"}
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:216:          detail={model.successRate === null ? (locale === "de" ? "ausstehende Ergebnisse" : "pending outcomes") : statusSummary}
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:219:          label={locale === "de" ? "Kosten" : "Cost"}
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:221:          detail={costPerCall !== null ? `${formatCost(costPerCall, locale)}/${locale === "de" ? "Aufruf" : "call"}` : locale === "de" ? "kein Preissignal" : "no pricing signal"}
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:226:          detail={costPerMillionTokens !== null ? (locale === "de" ? "gemischter Token-Satz" : "blended token rate") : locale === "de" ? "Preis nicht verfügbar" : "pricing unavailable"}
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:229:          label={locale === "de" ? "Ausgabegeschwindigkeit" : "Output Velocity"}
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:231:          detail={efficiency.outputTokensPerSecond === null ? (locale === "de" ? "keine aktive Ausgabe" : "no active output") : locale === "de" ? "generierte Ausgabe" : "generated output"}
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:234:          label={locale === "de" ? "Cache-Rate" : "Cache Rate"}
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:236:          detail={`${formatTokens(model.usage.cachedInputTokens, locale)} ${locale === "de" ? "im Cache" : "cached"}`}
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:241:          detail={`${formatTokens(model.usage.reasoningOutputTokens, locale)} ${locale === "de" ? "Schlussfolgerung" : "reasoning"}`}
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:246:          detail={locale === "de" ? "Generierungsverhältnis" : "generation ratio"}
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:252:          <div className="text-[10px] font-bold uppercase tracking-[0.16em] text-[color:var(--stats-label-color)]">{locale === "de" ? "Ergebnismix" : "Outcome Mix"}</div>
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:255:            {locale === "de" ? "Zuletzt aktiv" : "Last active"} {formatDateTime(model.lastActivityAt, locale)}
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:260:            <div className="text-[10px] font-bold uppercase tracking-[0.16em] text-[color:var(--stats-label-color)]">{locale === "de" ? "Token-Fluss-Struktur" : "Token-Flow Anatomy"}</div>
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:261:            <div className="text-[10px] font-bold uppercase tracking-[0.14em] text-[color:var(--stats-label-color)]">{formatTokens(model.usage.totalTokens, locale)} {locale === "de" ? "gesamt" : "total"}</div>
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:273:            <span>{locale === "de" ? "Eingabe" : "In"} {formatTokens(model.usage.inputTokens, locale)}</span>
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:275:            <span>{locale === "de" ? "Ausgabe" : "Out"} {formatTokens(model.usage.outputTokens, locale)}</span>
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:276:            <span>{locale === "de" ? "Denken" : "Think"} {formatTokens(model.usage.reasoningOutputTokens, locale)}</span>
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:311:            <div className={`text-[10px] font-bold uppercase tracking-[0.18em] ${TEXT_LABEL_CLASS}`}>{locale === "de" ? "Modellintelligenz" : "Model Intelligence"}</div>
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:312:            <div className={`mt-1 break-words text-xl font-semibold ${TEXT_VALUE_CLASS}`}>{locale === "de" ? "Modellleistung und Effizienz" : "Model performance & efficiency"}</div>
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:314:              {locale === "de" ? "Modellbezogene Telemetrie im ausgewählten Zeitraum – Token-Volumen, Zuverlässigkeit, Latenzverteilung, Cache-Effizienz und Ausgabegeschwindigkeit für jedes beteiligte Modell." : "Per-model telemetry across the selected window — token volume, reliability, latency distribution, cache efficiency, and output velocity for every model that participated."}
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:325:          <div className="mt-4 text-base font-semibold text-[color:var(--stats-value-color)]">{locale === "de" ? "Noch keine Modelltelemetrie" : "No model telemetry yet"}</div>
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:327:            {locale === "de" ? "Dieser Zeitraum enthält keine Modelleinträge. Vergleiche zu Volumen, Latenz, Cache und Schlussfolgerung erscheinen, sobald Anbieteraufrufe erfasst wurden." : "This window has no model entries, so volume, latency, cache, and reasoning comparisons will appear after provider invocations are recorded."}
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:334:              title={locale === "de" ? "Modellanteil" : "Model Share"}
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:335:              eyebrow={locale === "de" ? "Verteilung" : "Distribution"}
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:336:              description={locale === "de" ? "Token-Volumen auf die in diesem Zeitraum aktiven Modelle verteilt und in sichtbaren Bereichen gruppiert." : "Token volume split across the models active in this window, grouped into visible lanes."}
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:338:              centerLabel={locale === "de" ? (models.length === 1 ? "Modell" : "Modelle") : (models.length === 1 ? "model" : "models")}
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:344:                <div className="text-[10px] font-bold uppercase tracking-[0.2em] text-[color:var(--stats-label-color)]">{locale === "de" ? "Effizienz-Highlights" : "Efficiency Highlights"}</div>
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:349:                  label={locale === "de" ? "Volumenführer" : "Volume Leader"}
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:355:                  label={locale === "de" ? "Schnellstes" : "Fastest"}
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:361:                  label={locale === "de" ? "Zuverlässigstes" : "Most Reliable"}
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:367:                  label={locale === "de" ? "Beste Cache-Effizienz" : "Best Cache Efficiency"}
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:373:                  label={locale === "de" ? "Höchste Geschwindigkeit" : "Highest Velocity"}
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:379:                  label={locale === "de" ? "Höchster Schlussfolgerungsanteil" : "Highest Reasoning"}
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:388:                {locale === "de" ? "Zeitraumvolumen" : "Window Volume"}
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:391:              <div className="mt-1 text-xs font-bold uppercase tracking-[0.14em] text-[color:var(--stats-label-color)]">{locale === "de" ? "Tokens nach Modellvolumen sortiert" : "tokens ranked by model volume"}</div>
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:393:                <ModelMetric label={locale === "de" ? "Aufrufe" : "Calls"} value={formatNumber(totalCalls)} detail={totalCalls < LOW_SAMPLE_THRESHOLD ? (locale === "de" ? "kleine Stichprobe" : "sparse sample") : locale === "de" ? "Aufrufe" : "invocations"} />
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:394:                <ModelMetric label={locale === "de" ? "Ausgabe" : "Output"} value={formatTokens(totalOutput, locale)} detail={locale === "de" ? "generiert" : "generated"} />
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:395:                <ModelMetric label={locale === "de" ? "Im Cache" : "Cached"} value={formatTokens(totalCached, locale)} detail={locale === "de" ? "Eingabewiederverwendung" : "input reuse"} />
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:396:                <ModelMetric label={locale === "de" ? "Schlussfolgerung" : "Reasoning"} value={formatTokens(totalReasoning, locale)} detail={locale === "de" ? "Denk-Tokens" : "thinking tokens"} />
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:397:                <ModelMetric label={locale === "de" ? "Kosten" : "Cost"} value={formatPricingValue(totalCost, locale)} detail={totalCost > 0 ? (locale === "de" ? "bepreiste Nutzung" : "priced usage") : locale === "de" ? "kein Preissignal" : "no pricing signal"} />
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:401:                  ? (locale === "de" ? "Noch kein Modell hat Laufzeitstichproben; Latenz-Highlights und p50/p95-Felder bleiben absichtlich leer." : "No model has duration samples yet; latency highlights and p50/p95 cells stay intentionally empty.")
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:402:                  : (locale === "de" ? `${formatNumber(sampledModels)} von ${formatNumber(models.length)} Modellen haben Laufzeitstichproben. Highlights bevorzugen Modelle mit mindestens ${formatNumber(LOW_SAMPLE_THRESHOLD)} Aufrufen.` : `${formatNumber(sampledModels)} of ${formatNumber(models.length)} models have duration samples. Highlights prefer models with at least ${formatNumber(LOW_SAMPLE_THRESHOLD)} calls.`)}
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:410:                <div className="text-[10px] font-bold uppercase tracking-[0.2em] text-[color:var(--stats-label-color)]">{locale === "de" ? "Modellrangliste" : "Model Leaderboard"}</div>
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:412:                  {locale === "de" ? "Nach Token-Volumen und anschließend Modellbezeichnung sortiert. Jeder Eintrag zeigt Zuverlässigkeit, p50/p95-Latenz, Geschwindigkeit, Cache, Schlussfolgerung und Token-Struktur für den Vergleich." : "Ranked by token volume, then model label. Each entry keeps reliability, p50/p95 latency, speed, cache, reasoning, and token anatomy visible for comparison."}
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:416:                {locale === "de" ? "Sortierung: Tokens absteigend" : "Sort: tokens desc"}
dashboard/src/v2/pages/stats/components/ModelsStudio.tsx:421:                {locale === "de" ? "Modelle sind vorhanden, aber keines hat in diesem Zeitraum Token-Volumen gemeldet. Bis Nutzungssummen eintreffen, erfolgt die Sortierung nach Bezeichnungen." : "Models are present, but none reported token volume in this window. Ranking falls back to labels until usage totals arrive."}
dashboard/src/v2/pages/stats/stats-utils.ts:61:    return `${new Intl.NumberFormat(locale, { minimumFractionDigits: 2, maximumFractionDigits: 2 }).format(value / 1_000_000)}${locale === "de" ? " Mio." : "M"}`;
dashboard/src/v2/pages/stats/stats-utils.ts:87:    return locale === "de" ? `${number.format(hours)} h ${number.format(minutes)} min` : `${number.format(hours)}h ${number.format(minutes)}m`;
dashboard/src/v2/pages/stats/stats-utils.ts:90:    return locale === "de" ? `${number.format(minutes)} min ${number.format(remainingSeconds)} s` : `${number.format(minutes)}m ${number.format(remainingSeconds)}s`;
dashboard/src/v2/pages/stats/stats-utils.ts:92:  return locale === "de" ? `${number.format(remainingSeconds)} s` : `${number.format(remainingSeconds)}s`;
dashboard/src/v2/pages/stats/stats-utils.ts:116:    return locale === "de" ? "Noch keine Aktivität" : "No activity yet";
dashboard/src/v2/pages/stats/stats-utils.ts:206:    fallbackLabel: locale === "de" ? "Andere Anbieter" : "Other providers",
dashboard/src/v2/pages/stats/stats-utils.ts:228:      label: locale === "de" ? "Eingabe" : "Input",
dashboard/src/v2/pages/stats/stats-utils.ts:234:      label: locale === "de" ? "Im Cache" : "Cached",
dashboard/src/v2/pages/stats/stats-utils.ts:240:      label: locale === "de" ? "Ausgabe" : "Output",
dashboard/src/v2/pages/stats/stats-utils.ts:246:      label: locale === "de" ? "Schlussfolgerung" : "Reasoning",
dashboard/src/v2/pages/stats/model-insights.ts:111:      label: locale === "de" ? "Nicht verfügbar" : "Unavailable",
dashboard/src/v2/pages/stats/model-insights.ts:113:      detail: locale === "de" ? "Für diesen Zeitraum wurden keine Telemetriezahlen erfasst." : "No telemetry counts were recorded for this window.",
dashboard/src/v2/pages/stats/model-insights.ts:114:      caveat: locale === "de" ? "Es gibt noch keine Aufrufquellen-Telemetrie zum Vergleichen." : "There is no invocation-source telemetry to compare yet.",
dashboard/src/v2/pages/stats/model-insights.ts:120:  const reportedLabel = locale === "de" ? `${number.format(mix.reported)} gemeldet` : `${number.format(mix.reported)} reported`;
dashboard/src/v2/pages/stats/model-insights.ts:122:    mix.estimated > 0 ? (locale === "de" ? `${number.format(mix.estimated)} geschätzt` : `${number.format(mix.estimated)} estimated`) : null,
dashboard/src/v2/pages/stats/model-insights.ts:123:    mix.unavailable > 0 ? (locale === "de" ? `${number.format(mix.unavailable)} nicht verfügbar` : `${number.format(mix.unavailable)} unavailable`) : null,
dashboard/src/v2/pages/stats/model-insights.ts:124:    mix.unsupported > 0 ? (locale === "de" ? `${number.format(mix.unsupported)} nicht unterstützt` : `${number.format(mix.unsupported)} unsupported`) : null,
dashboard/src/v2/pages/stats/model-insights.ts:128:    ? (locale === "de" ? "Gemeldet" : "Reported")
dashboard/src/v2/pages/stats/model-insights.ts:130:      ? (locale === "de" ? "Geschätzt" : "Estimated")
dashboard/src/v2/pages/stats/model-insights.ts:132:        ? (locale === "de" ? "Nicht verfügbar" : "Unavailable")
dashboard/src/v2/pages/stats/model-insights.ts:134:          ? (locale === "de" ? "Nicht unterstützt" : "Unsupported")
dashboard/src/v2/pages/stats/model-insights.ts:135:          : (locale === "de" ? "Unbekannt" : "Unknown");
dashboard/src/v2/pages/stats/model-insights.ts:154:    ? (locale === "de" ? "Für diesen Zeitraum fehlen Telemetriezahlen." : "Telemetry counts are missing for this window.")
dashboard/src/v2/pages/stats/model-insights.ts:156:      ? (locale === "de" ? "Alle gezählten Aufrufe wurden direkt gemeldet." : "All counted invocations were reported directly.")
dashboard/src/v2/pages/stats/model-insights.ts:158:        ? (locale === "de" ? "Ersatzwerte und nicht unterstützte Quellen sind enthalten; die gemeldete Qualität ist daher nur teilweise vollständig." : "Fallback and unsupported sources are included in the count, so reported quality is only partial.")
dashboard/src/v2/pages/stats/model-insights.ts:159:        : (locale === "de" ? "Gemeldete Werte dominieren diesen Zeitraum." : "Reported counts dominate this window.");
dashboard/src/v2/pages/stats/model-insights.ts:225:          value: locale === "de" ? `${formatCompactTokens(busiest.usage.totalTokens, locale)} Tokens` : `${formatCompactTokens(busiest.usage.totalTokens, locale)} tokens`,
dashboard/src/v2/pages/stats/model-insights.ts:226:          detail: locale === "de" ? `${new Intl.NumberFormat(locale).format(busiest.usage.invocationCount)} Aufrufe` : `${new Intl.NumberFormat(locale).format(busiest.usage.invocationCount)} calls`,
dashboard/src/v2/pages/stats/model-insights.ts:232:          value: `${formatCompactDuration(fastest.duration.p50Ms, locale)} ${locale === "de" ? "Median" : "median"}`,
dashboard/src/v2/pages/stats/model-insights.ts:239:          value: locale === "de" ? `${formatSuccessRate(mostReliable.successRate, locale)} erfolgreich` : `${formatSuccessRate(mostReliable.successRate, locale)} success`,
dashboard/src/v2/pages/stats/model-insights.ts:240:          detail: locale === "de" ? `${new Intl.NumberFormat(locale).format(mostReliable.statusCounts.failed)} fehlgeschlagen` : `${new Intl.NumberFormat(locale).format(mostReliable.statusCounts.failed)} failed`,
dashboard/src/v2/pages/stats/model-insights.ts:246:          value: locale === "de" ? `${new Intl.NumberFormat(locale, { style: "percent" }).format(computeUsageEfficiency(bestCache.usage).cacheHitRate ?? 0)} Cache-Treffer` : `${new Intl.NumberFormat(locale, { style: "percent" }).format(computeUsageEfficiency(bestCache.usage).cacheHitRate ?? 0)} cache hits`,
dashboard/src/v2/pages/stats/model-insights.ts:247:          detail: locale === "de" ? `${formatCompactTokens(bestCache.usage.cachedInputTokens, locale)} im Cache` : `${formatCompactTokens(bestCache.usage.cachedInputTokens, locale)} cached`,
dashboard/src/v2/pages/stats/model-insights.ts:290:  return { model: best, value: `${new Intl.NumberFormat(locale).format(velocity)} ${locale === "de" ? "Tok./s" : "tok/s"}`, detail: `${formatCompactDuration(best.usage.activeTimeMs, locale)} ${locale === "de" ? "aktiv" : "active"}` };
dashboard/src/v2/pages/stats/model-insights.ts:313:  return { model: best, value: locale === "de" ? `${formattedShare} Schlussfolgerung` : `${formattedShare} reasoning`, detail: locale === "de" ? `${formatCompactTokens(best.usage.reasoningOutputTokens, locale)} Schlussfolgerungs-Tokens` : `${formatCompactTokens(best.usage.reasoningOutputTokens, locale)} reasoning tokens` };
dashboard/src/v2/pages/stats/model-insights.ts:333:      label: locale === "de" ? "Andere Modelle" : "Other models",
dashboard/src/v2/pages/stats/model-insights.ts:345:    return `${new Intl.NumberFormat(locale, { minimumFractionDigits: 2, maximumFractionDigits: 2 }).format(value / 1_000_000)}${locale === "de" ? " Mio." : "M"}`;
dashboard/src/v2/pages/stats/model-insights.ts:359:    return locale === "de" ? `${hours} Std. ${minutes} Min.` : `${hours}h ${minutes}m`;
dashboard/src/v2/pages/stats/model-insights.ts:362:    return locale === "de" ? `${minutes} Min. ${remainingSeconds} Sek.` : `${minutes}m ${remainingSeconds}s`;
dashboard/src/v2/pages/stats/model-insights.ts:364:  return locale === "de" ? `${remainingSeconds} Sek.` : `${remainingSeconds}s`;
dashboard/src/v2/pages/stats/stats-page-view-model.ts:31:  let completionConfidence = locale === "de" ? "Nicht verfügbar" : "Unavailable";
dashboard/src/v2/pages/stats/stats-page-view-model.ts:33:    completionConfidence = locale === "de" ? "Keine Telemetrie" : "No telemetry";
dashboard/src/v2/pages/stats/stats-page-view-model.ts:35:    completionConfidence = locale === "de" ? "Vom Anbieter gemeldet" : "Provider reported";
dashboard/src/v2/pages/stats/stats-page-view-model.ts:37:    completionConfidence = locale === "de" ? "Gemeldet und Ersatzwerte gemischt" : "Mixed reported + fallback";
dashboard/src/v2/pages/stats/stats-page-view-model.ts:39:    completionConfidence = locale === "de" ? "Geschätzte Ersatzwerte" : "Estimated fallback";
dashboard/src/v2/pages/stats/chart-view-models.ts:45:      return locale === "de" ? "Kern" : "Core";
dashboard/src/v2/pages/stats/chart-view-models.ts:47:      return locale === "de" ? "Nutzung" : "Usage";
dashboard/src/v2/pages/stats/chart-view-models.ts:49:      return locale === "de" ? "Summen" : "Totals";
dashboard/src/v2/pages/stats/chart-view-models.ts:51:      return locale === "de" ? "Token-Details" : "Token details";
dashboard/src/v2/pages/stats/chart-view-models.ts:53:      return locale === "de" ? "Quellenvertrauen" : "Source confidence";
dashboard/src/v2/pages/stats/chart-view-models.ts:55:      return locale === "de" ? "Anbieter" : "Providers";
dashboard/src/v2/pages/stats/chart-view-models.ts:57:      return locale === "de" ? "Anbieterkosten" : "Provider costs";
dashboard/src/v2/pages/stats/chart-view-models.ts:59:      return locale === "de" ? "Modelle" : "Models";
dashboard/src/v2/pages/stats/chart-view-models.ts:61:      return locale === "de" ? "Modellkosten" : "Model costs";
dashboard/src/v2/pages/stats/chart-view-models.ts:64:      return locale === "de" ? "Zweckzeit" : "Purpose time";
dashboard/src/v2/pages/stats/chart-view-models.ts:66:      return locale === "de" ? "Zweckaufrufe" : "Purpose calls";
dashboard/src/v2/pages/stats/chart-view-models.ts:164:      signalLabel: series.signalLabel || (locale === "de" ? "Kennzahl" : "Metric"),
dashboard/src/v2/pages/stats/chart-view-models.ts:249:    : locale === "de" ? "Keine aktiven Reihen" : "No active series";
dashboard/src/v2/pages/stats/chart-view-models.ts:252:  return locale === "de" ? [
dashboard/src/v2/pages/stats/trend-insights.ts:36:    return delta.current > 0 ? (locale === "de" ? "neu" : "new") : "—";
dashboard/src/v2/pages/stats/trend-insights.ts:40:    return locale === "de" ? "unverändert" : "flat";
dashboard/src/v2/components/stats/TopCardsModeRenderer.tsx:540:          signalLabel={locale === "de" ? "Latenz" : "Latency"}
dashboard/src/v2/components/stats/StatsMetricCard.tsx:52:        signal: locale === "de" ? signalLabel : signalLabel.toLowerCase(),
dashboard/src/v2/components/stats/StatsMetricCard.tsx:57:    : text("noSparklineSummary", { label, signal: locale === "de" ? signalLabel : signalLabel.toLowerCase() });
