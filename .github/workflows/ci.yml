name: CI

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: ["**"]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: self-hosted

    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.33.0
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Restore Vite and Vitest caches
        uses: actions/cache@v4
        with:
          path: |
            .cache/vite
            .cache/vitest
            .cache/tsc
          key: ${{ runner.os }}-node-22-tooling-${{ hashFiles('pnpm-lock.yaml', 'vite.config.ts', 'vitest.config.ts', 'tsconfig.json', 'dashboard/tsconfig.json') }}
          restore-keys: |
            ${{ runner.os }}-node-22-tooling-

      - name: Run consolidated CI verification
        run: pnpm run ci

      - name: Audit dependencies
        run: pnpm run audit
