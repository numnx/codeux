# Task Template

## Overview
Briefly describe the purpose of this task or procedure. What will the user accomplish by the end of this guide?

::: info
Use an info block to call out important context that a user needs before starting.
:::

## Prerequisites
List any requirements needed before starting:
* Installed software (e.g., Node.js v18+)
* Required permissions or access tokens
* Familiarity with specific concepts

::: warning
Use a warning block for critical prerequisites that will cause the procedure to fail if ignored.
:::

## Steps

1. **Step One Title**
   Describe the exact action the user must take. Keep instructions clear, direct, and actionable.

   ```bash
   pnpm run build
   ```

2. **Step Two Title**
   Provide additional steps logically. Do not combine multiple unrelated actions into one step.

::: tip
Use a tip block for best practices, shortcuts, or alternative methods that can make the process easier.
:::

## Expected Result
Explain how the user can verify they have completed the task successfully.

* E.g., The terminal should output `Build successful`.
* E.g., You should see the new component rendered on the dashboard.

::: danger
Use a danger block sparingly for irreversible actions or potential data loss warnings.
:::