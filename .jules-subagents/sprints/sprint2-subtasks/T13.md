title: Consolidate Subtask Merge Persistence into SubtaskFileRepository
depends_on: [T12]
is_independent: true
merged: false
prompt:
# Task Specification: T13 - Sprint 2

## Objective
Remove direct markdown file writes from CI gate service and route all merge-flag persistence through `SubtaskFileRepository`.

## Files to Modify
- `src/domain/sprint/ci/feature-pr-gate.ts`: Remove direct fs/path merge writes.
- `src/infrastructure/repositories/subtask-file-repository.ts`: Ensure `setMerged` is the only merge persistence API.
- `tests/backend/domain/sprint/ci/feature-pr-gate.test.ts`: Update persistence interaction tests.

## Technical Details & Research Findings
- **Context**: direct file writes bypass parser/stringify rules and duplicate repository behavior.
- **Goal**: one persistence path for subtask metadata updates.
- **Constraint**: maintain resilient runtime behavior if persistence fails.

## Execution Steps
1. Inject repository dependency into feature gate service for merge writes.
2. Replace local `persistTaskMergedFlag` logic with repository calls.
3. Keep failure handling semantics consistent (runtime status still advances if configured).
4. Add/adjust tests for repository call behavior.

## Verification Requirements
- **Automated Tests**: `npm run test -- tests/backend/domain/sprint/ci/feature-pr-gate.test.ts`; `npm run test -- tests/backend/infrastructure/repositories/subtask-file-repository.test.ts`
- **Criteria**: No direct markdown mutation remains in CI gate service.
- **Criteria**: `SubtaskFileRepository` is the single write path for `merged` updates.

## Engineering Standard
- Use the feature branch: feature/sprint2-implementation
- Ensure all tests pass before completing.
