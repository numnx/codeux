title: Replace Sync Branch Preflight Commands with Async Runner
depends_on: [T16]
is_independent: true
merged: false
prompt:
# Task Specification: T18 - Sprint 2

## Objective
Reimplement branch preflight checks using the shared async command runner to remove sync shell execution from orchestration preflight.

## Files to Modify
- `src/sprint/steps/branch-preflight-step.ts`: Replace `execFileSync` and sync fs calls.
- `src/shared/subprocess/command-runner.ts`: Reuse existing command abstraction where needed.
- `tests/backend/sprint/preflight-logic.test.ts`: Update for async branch preflight behavior.

## Technical Details & Research Findings
- **Context**: sync process calls block event loop and are inconsistent with standardized subprocess utilities.
- **Goal**: async, timeout-aware, structured failure handling for preflight checks.
- **Constraint**: preserve branch existence semantics and guardrail behavior.

## Execution Steps
1. Convert preflight helpers to async command execution.
2. Replace sync fs checks with async equivalents where applicable.
3. Propagate async branch preflight into orchestrator call path safely.
4. Update tests to verify behavior parity.

## Verification Requirements
- **Automated Tests**: `npm run test -- tests/backend/sprint/preflight-logic.test.ts`; `npm run typecheck`
- **Criteria**: No `execFileSync` usage remains in branch preflight path.
- **Criteria**: Preflight decisions remain behavior-compatible.

## Engineering Standard
- Use the feature branch: feature/sprint2-implementation
- Ensure all tests pass before completing.
