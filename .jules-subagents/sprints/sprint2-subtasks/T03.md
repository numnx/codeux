title: Remove any from Runtime Context + Dependency Factory Interfaces
depends_on: [T02]
is_independent: true
merged: false
prompt:
# Task Specification: T03 - Sprint 2

## Objective
Refactor `src/app/dependency-factory.ts` context/dependency interfaces to remove `any` and enforce strongly typed runtime wiring.

## Files to Modify
- `src/app/dependency-factory.ts`: Replace `any` fields in `ServerContext` and dependent callbacks.
- `src/server/jules-agent-server.ts`: Align context providers with stricter interfaces.
- `tests/backend/mcp/agent-tool-handler.test.ts`: Update test doubles for typed dependencies.

## Technical Details & Research Findings
- **Context**: Composition root interfaces define reliability boundaries; loose types propagate across most modules.
- **Focus**: status payload type, CI status scope args, merge persistence args, and runtime lookup callbacks.
- **Constraint**: Keep dependency behavior unchanged while improving compile-time validation.

## Execution Steps
1. Introduce dedicated interface types for context payloads currently represented as `any`.
2. Update factory signatures and server context implementation to satisfy strict types.
3. Refactor callsites that rely on implicit shape assumptions.
4. Run full typecheck to validate no hidden incompatible dependencies remain.

## Verification Requirements
- **Automated Tests**: `npm run typecheck`; `npm run test -- tests/backend/mcp/agent-tool-handler.test.ts`
- **Criteria**: No `any` remains in dependency-factory interfaces.
- **Criteria**: Runtime dependency graph compiles with strict contracts.

## Engineering Standard
- Use the feature branch: feature/sprint2-implementation
- Ensure all tests pass before completing.
