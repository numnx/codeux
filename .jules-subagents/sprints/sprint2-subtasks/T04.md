title: Break settings-sanitizer.ts into Domain Sanitizers
depends_on: [T02]
is_independent: true
merged: false
prompt:
# Task Specification: T04 - Sprint 2

## Objective
Decompose `src/repositories/settings-sanitizer.ts` into focused domain sanitizer modules while preserving existing setting normalization behavior.

## Files to Modify
- `src/repositories/settings-sanitizer.ts`: Reduce to orchestration/composition.
- `src/domain/settings/settings-sanitizers/ai-provider-sanitizer.ts`: Create module.
- `src/domain/settings/settings-sanitizers/git-sanitizer.ts`: Create module.
- `src/domain/settings/settings-sanitizers/ci-sanitizer.ts`: Create module.
- `src/domain/settings/settings-sanitizers/sprint-loop-sanitizer.ts`: Create module.
- `src/domain/settings/settings-sanitizers/cli-workflow-sanitizer.ts`: Create module.

## Technical Details & Research Findings
- **Context**: `sanitizeSettings` is currently one of the largest hotspot functions (243 lines).
- **Refactor target**: isolate normalization by domain to simplify test scope and future changes.
- **Constraint**: external hints fallback semantics and defaults must remain behavior-compatible.

## Execution Steps
1. Extract normalization logic by settings domain into dedicated modules with explicit inputs/outputs.
2. Make top-level sanitizer a pure orchestrator that composes domain sanitizers.
3. Retain existing bounds checks and legacy fallback semantics.
4. Split/add tests by domain sanitizer behavior.

## Verification Requirements
- **Automated Tests**: `npm run test -- tests/backend/repositories/settings-repository.test.ts`; `npm run typecheck`
- **Criteria**: Top-level settings sanitizer is significantly smaller and orchestration-focused.
- **Criteria**: Normalization behavior matches pre-refactor outputs for existing fixtures.

## Engineering Standard
- Use the feature branch: feature/sprint2-implementation
- Ensure all tests pass before completing.
