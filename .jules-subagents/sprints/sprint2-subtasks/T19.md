title: Upgrade Subtask Parser to Strict Frontmatter-Compatible Grammar
depends_on: [T13]
is_independent: true
merged: false
prompt:
# Task Specification: T19 - Sprint 2

## Objective
Implement parser v2 for subtask markdown with robust key parsing, quoted dependency arrays, and deterministic serialization.

## Files to Modify
- `src/infrastructure/repositories/subtask-parser.ts`: Replace current parser implementation with strict grammar logic.
- `tests/backend/infrastructure/repositories/subtask-parser.test.ts`: Expand edge-case coverage.

## Technical Details & Research Findings
- **Context**: current parser is regex/split-driven and brittle for quoting/spacing variants.
- **Goal**: reliable parse/stringify round-trip for sprint subtask metadata.
- **Constraint**: preserve existing field names and compatible output structure.

## Execution Steps
1. Define strict parsing rules for metadata block and prompt section.
2. Support quoted/unquoted dependency entries and whitespace normalization.
3. Implement deterministic `stringify` ordering and formatting.
4. Add comprehensive round-trip tests for edge cases.

## Verification Requirements
- **Automated Tests**: `npm run test -- tests/backend/infrastructure/repositories/subtask-parser.test.ts`; `npm run typecheck`
- **Criteria**: Parser handles edge-case metadata safely and predictably.
- **Criteria**: Round-trip parse/stringify tests pass for complex fixtures.

## Engineering Standard
- Use the feature branch: feature/sprint2-implementation
- Ensure all tests pass before completing.
