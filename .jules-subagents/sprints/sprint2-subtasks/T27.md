title: Unify Command Execution Stack and Fix Stream Line Framing
depends_on: [T18, T24]
is_independent: true
merged: false
prompt:
# Task Specification: T27 - Sprint 2

## Objective
Consolidate subprocess execution paths and implement robust line-buffer handling for streaming stdout/stderr callbacks.

## Files to Modify
- `src/shared/subprocess/command-runner.ts`: Improve line framing and shared execution behavior.
- `src/services/cli-process-runner.ts`: Align wrappers to unified command runner interface.
- `src/services/jules-source-resolver.ts`: Ensure consistent command execution pattern.
- `tests/backend/shared/subprocess/command-runner.test.ts`: Add partial-chunk line framing tests.

## Technical Details & Research Findings
- **Context**: command execution logic is split across wrappers; line callbacks can break on chunk boundaries.
- **Goal**: single reliable execution model for strict and streaming paths.
- **Constraint**: preserve existing command timeout/error contract behavior.

## Execution Steps
1. Introduce buffered line processing for stdout/stderr streams.
2. Unify wrappers around one command runner contract.
3. Migrate remaining divergent subprocess callsites.
4. Add tests for chunked output and stderr clipping behavior.

## Verification Requirements
- **Automated Tests**: `npm run test -- tests/backend/shared/subprocess/command-runner.test.ts`; `npm run typecheck`
- **Criteria**: Streaming callbacks emit correct line boundaries.
- **Criteria**: Subprocess callsites are standardized on one execution stack.

## Engineering Standard
- Use the feature branch: feature/sprint2-implementation
- Ensure all tests pass before completing.
