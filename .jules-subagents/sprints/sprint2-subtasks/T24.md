title: Decompose CliWorkflowService.runTaskWorkflow into Pipeline Steps
depends_on: [T06]
is_independent: true
merged: false
prompt:
# Task Specification: T24 - Sprint 2

## Objective
Refactor `runTaskWorkflow` into explicit pipeline stages for setup, provider execution, git finalization, PR finalization, and cleanup.

## Files to Modify
- `src/services/cli-workflow-service.ts`: Reduce orchestrator method and delegate to pipeline stages.
- `src/services/cli-workflow/pipeline/prepare-stage.ts`: Create module.
- `src/services/cli-workflow/pipeline/execute-provider-stage.ts`: Create module.
- `src/services/cli-workflow/pipeline/git-finalize-stage.ts`: Create module.
- `src/services/cli-workflow/pipeline/pr-finalize-stage.ts`: Create module.
- `src/services/cli-workflow/pipeline/cleanup-stage.ts`: Create module.

## Technical Details & Research Findings
- **Context**: existing method still mixes multiple responsibilities in one flow.
- **Goal**: stage-specific logic with typed step outputs improves readability and testability.
- **Constraint**: preserve session activity logging and retry semantics.

## Execution Steps
1. Define pipeline stage interfaces and context/result contracts.
2. Extract existing logic into stage modules without behavior drift.
3. Keep main workflow as high-level stage orchestrator.
4. Update/add tests for stage boundaries and failure propagation.

## Verification Requirements
- **Automated Tests**: `npm run test -- tests/backend/services/cli-workflow-service.test.ts`; `npm run typecheck`
- **Criteria**: Main `runTaskWorkflow` is under 70 lines.
- **Criteria**: Pipeline stages are isolated, typed, and behavior-compatible.

## Engineering Standard
- Use the feature branch: feature/sprint2-implementation
- Ensure all tests pass before completing.
