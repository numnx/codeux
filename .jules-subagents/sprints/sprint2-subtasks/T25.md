title: Remove Remaining Provider any Casts and Introduce Command Spec Map
depends_on: [T24]
is_independent: true
merged: false
prompt:
# Task Specification: T25 - Sprint 2

## Objective
Eliminate provider-runtime `any` casts and replace provider command branching with a typed command-spec map.

## Files to Modify
- `src/infrastructure/providers/cli/provider-runner.ts`: Remove `as any` and map provider command specs.
- `src/services/provider-routing.ts`: Remove `as any` in strategy selection flow.
- `tests/backend/services/provider-routing.test.ts`: Update strategy matrix tests if needed.

## Technical Details & Research Findings
- **Context**: provider-specific logic still includes `any` escape hatches that weaken type safety.
- **Goal**: declarative provider specs and strict typed strategy inputs.
- **Constraint**: provider command flags/env behavior must remain unchanged.

## Execution Steps
1. Introduce typed provider command specification map for CLI command assembly.
2. Refactor provider runner to use specs with strict provider labels and settings types.
3. Remove strategy task-cast `any` usage in provider routing.
4. Validate command and routing behavior through tests.

## Verification Requirements
- **Automated Tests**: `npm run test -- tests/backend/services/provider-routing.test.ts`; `npm run test -- tests/backend/services/cli-workflow-service.test.ts`; `npm run typecheck`
- **Criteria**: No `any` remains in provider runner/routing modules.
- **Criteria**: Provider selection and command execution behavior is preserved.

## Engineering Standard
- Use the feature branch: feature/sprint2-implementation
- Ensure all tests pass before completing.
