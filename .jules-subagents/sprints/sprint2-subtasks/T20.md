title: Improve Subtask Repository Load Path and Logging
depends_on: [T19]
is_independent: true
merged: false
prompt:
# Task Specification: T20 - Sprint 2

## Objective
Refactor `SubtaskFileRepository` to support stable-order parallel loading, structured parse warnings, and logger-based error reporting.

## Files to Modify
- `src/infrastructure/repositories/subtask-file-repository.ts`: Replace sequential load + `console.error` usage.
- `tests/backend/infrastructure/repositories/subtask-file-repository.test.ts`: Add warning/reporting and order guarantees.

## Technical Details & Research Findings
- **Context**: current repository loads subtasks sequentially and logs with direct console output.
- **Goal**: improve performance and align with structured logging policy.
- **Constraint**: keep malformed-file tolerance behavior unless explicitly changed.

## Execution Steps
1. Introduce logger dependency (optional with safe default behavior).
2. Load markdown files in parallel while preserving deterministic ordering in output.
3. Aggregate parse failures into structured warnings instead of raw console logging.
4. Update tests to validate ordering and warning semantics.

## Verification Requirements
- **Automated Tests**: `npm run test -- tests/backend/infrastructure/repositories/subtask-file-repository.test.ts`; `npm run typecheck`
- **Criteria**: No direct `console.*` usage remains in subtask repository.
- **Criteria**: Subtask loading is stable, parallelized, and test-covered.

## Engineering Standard
- Use the feature branch: feature/sprint2-implementation
- Ensure all tests pass before completing.
