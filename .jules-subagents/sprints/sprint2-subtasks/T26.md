title: Complete Docker Credential Mount Builder and Harden Runtime Path Handling
depends_on: [T24]
is_independent: true
merged: false
prompt:
# Task Specification: T26 - Sprint 2

## Objective
Replace simplified credential mount handling in Docker runner with complete, explicit, and tested mount builder logic.

## Files to Modify
- `src/infrastructure/providers/cli/docker-runner.ts`: Remove simplified inline mount block.
- `src/infrastructure/providers/cli/docker-credential-mount-builder.ts`: Create dedicated builder module.
- `tests/backend/infrastructure/providers/cli/docker-bootstrap-builder.test.ts`: Extend for mount matrix behavior.

## Technical Details & Research Findings
- **Context**: Docker credential mounts are security-sensitive and currently include a simplification placeholder.
- **Goal**: explicit per-provider mount decisions with clear path resolution and read-only guarantees.
- **Constraint**: maintain existing user-facing settings toggles and mount path semantics.

## Execution Steps
1. Create a dedicated credential mount builder with typed inputs and outputs.
2. Move mount resolution/validation logic out of Docker runner method body.
3. Ensure all mount permutations are handled and logged consistently.
4. Add tests for toggle matrix and missing-path behavior.

## Verification Requirements
- **Automated Tests**: `npm run test -- tests/backend/infrastructure/providers/cli/docker-bootstrap-builder.test.ts`; `npm run typecheck`
- **Criteria**: No simplification placeholder remains in Docker credential logic.
- **Criteria**: Credential mount behavior is explicit, complete, and test-covered.

## Engineering Standard
- Use the feature branch: feature/sprint2-implementation
- Ensure all tests pass before completing.
