title: Strengthen /health and /ready to Real Subsystem Probes
depends_on: [T08]
is_independent: true
merged: false
prompt:
# Task Specification: T09 - Sprint 2

## Objective
Upgrade health/readiness endpoints to report explicit subsystem probe status instead of minimal timestamp-based readiness.

## Files to Modify
- `src/server/dashboard-server.ts`: Extend `/health` and `/ready` payloads and probe integration.
- `src/server/jules-agent-server.ts`: Provide readiness probe inputs (settings DB, MCP connection, dashboard binding state).
- `tests/backend/server/dashboard-server.test.ts`: Add probe-based readiness tests.

## Technical Details & Research Findings
- **Context**: readiness currently maps mostly to in-memory orchestration status state.
- **Goal**: accurate deploy/runtime checks for operational reliability.
- **Constraint**: keep endpoint contracts stable enough for current dashboard consumers.

## Execution Steps
1. Define readiness probe model with component states and aggregate status.
2. Wire probes for settings DB availability, dashboard runtime bind, and MCP service readiness.
3. Return structured JSON for each endpoint with explicit component results.
4. Add tests for ready/not-ready component combinations.

## Verification Requirements
- **Automated Tests**: `npm run test -- tests/backend/server/dashboard-server.test.ts`; `npm run typecheck`
- **Criteria**: `/health` and `/ready` return structured component-level status.
- **Criteria**: Not-ready conditions are deterministic and test-covered.

## Engineering Standard
- Use the feature branch: feature/sprint2-implementation
- Ensure all tests pass before completing.
