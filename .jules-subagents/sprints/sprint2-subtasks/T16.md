title: Refactor SprintOrchestrator.execute into Action Handlers
depends_on: [T15]
is_independent: true
merged: false
prompt:
# Task Specification: T16 - Sprint 2

## Objective
Split `SprintOrchestrator.execute` into dedicated action handlers (`plan`, `status`, `orchestrate`) and shared report composition utilities.

## Files to Modify
- `src/sprint/sprint-orchestrator.ts`: Reduce `execute` to high-level dispatch.
- `src/domain/sprint/orchestrator/sprint-action-runner.ts`: Create action-specific orchestration module.
- `tests/backend/sprint/sprint-orchestrator.test.ts`: Update to new action routing internals.

## Technical Details & Research Findings
- **Context**: `execute` still handles preflight, action branching, watch setup, and report persistence directly.
- **Goal**: isolate action behavior and simplify future changes.
- **Constraint**: keep current request/response contract and report text structure stable.

## Execution Steps
1. Extract action-specific handlers and common preflight/report helpers.
2. Refactor execute method to route actions through extracted handlers.
3. Keep status persistence and guidance append logic centralized but decoupled.
4. Update tests to validate unchanged external behavior.

## Verification Requirements
- **Automated Tests**: `npm run test -- tests/backend/sprint/sprint-orchestrator.test.ts`; `npm run test -- tests/backend/sprint/preflight-logic.test.ts`
- **Criteria**: `execute` is reduced to a concise router.
- **Criteria**: Action-specific flows are modular and tested.

## Engineering Standard
- Use the feature branch: feature/sprint2-implementation
- Ensure all tests pass before completing.
