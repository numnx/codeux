title: Remove Duplicate Feature CI Gate Logic from CycleRunner
depends_on: [T10]
is_independent: true
merged: false
prompt:
# Task Specification: T12 - Sprint 2

## Objective
Eliminate duplicated feature CI gate implementation from `cycle-runner.ts` and use `FeaturePrGateService` as the single source of CI gating policy.

## Files to Modify
- `src/domain/sprint/orchestrator/cycle-runner.ts`: Remove inlined `applyFeatureBranchCiGate` implementation.
- `src/domain/sprint/ci/feature-pr-gate.ts`: Use as exclusive CI gate execution path.
- `tests/backend/sprint/ci-integration.test.ts`: Validate behavior parity after deduplication.

## Technical Details & Research Findings
- **Context**: duplicate CI logic in two places is a high-risk divergence source.
- **Goal**: centralize policy behavior and simplify future CI gate changes.
- **Constraint**: keep existing orchestration output/report behavior equivalent.

## Execution Steps
1. Delete duplicated gate method from cycle runner.
2. Inject/route all CI gate calls through `FeaturePrGateService.evaluateCiGate`.
3. Ensure retry count and notification state is still wired correctly.
4. Run integration tests and compare output behavior.

## Verification Requirements
- **Automated Tests**: `npm run test -- tests/backend/sprint/ci-integration.test.ts`; `npm run test -- tests/backend/domain/sprint/ci/feature-pr-gate.test.ts`
- **Criteria**: There is only one feature CI gate implementation path.
- **Criteria**: Orchestration CI behavior remains unchanged from caller perspective.

## Engineering Standard
- Use the feature branch: feature/sprint2-implementation
- Ensure all tests pass before completing.
