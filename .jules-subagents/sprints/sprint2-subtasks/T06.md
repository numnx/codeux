title: Decompose createRuntimeDependencies into Sub-Factories
depends_on: [T03]
is_independent: true
merged: false
prompt:
# Task Specification: T06 - Sprint 2

## Objective
Split `createRuntimeDependencies` into subsystem-specific factories to reduce composition complexity and improve testability.

## Files to Modify
- `src/app/dependency-factory.ts`: Reduce to top-level composition.
- `src/app/dependency-factory/core-factory.ts`: Create module.
- `src/app/dependency-factory/sprint-factory.ts`: Create module.
- `src/app/dependency-factory/mcp-factory.ts`: Create module.
- `src/app/dependency-factory/dashboard-factory.ts`: Create module.

## Technical Details & Research Findings
- **Context**: Main factory is 180 lines and wires all domains inline.
- **Goal**: explicit subsystem boundaries with typed outputs per factory.
- **Constraint**: preserve existing dependency behavior and initialization order.

## Execution Steps
1. Define sub-factory interfaces and outputs for each subsystem.
2. Move wiring code into focused modules and keep main factory as orchestrator.
3. Ensure shared dependencies (logger, settings, session tracking) are passed consistently.
4. Update tests/mocks to target new factory structure.

## Verification Requirements
- **Automated Tests**: `npm run typecheck`; `npm run test -- tests/backend/smoke.test.ts`
- **Criteria**: Main dependency factory is under 80 lines.
- **Criteria**: Factory decomposition does not change runtime behavior.

## Engineering Standard
- Use the feature branch: feature/sprint2-implementation
- Ensure all tests pass before completing.
