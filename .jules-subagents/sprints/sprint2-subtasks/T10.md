title: Unify MCP Tool Contracts End-to-End
depends_on: [T02]
is_independent: true
merged: false
prompt:
# Task Specification: T10 - Sprint 2

## Objective
Remove remaining MCP dispatch `any` usage by unifying tool definitions, dispatch types, and router expectations.

## Files to Modify
- `src/contracts/mcp-tool-definitions.ts`: Replace `ToolHandlerMap`/`dispatchTool` `any` contracts.
- `src/api/mcp/tool-registry.ts`: Align handler/dispatch types with tool contracts.
- `src/server/mcp-request-router.ts`: Remove unsafe `as any` cast from tool definition return path.

## Technical Details & Research Findings
- **Context**: typed registry exists, but contract and router still allow unsafe casts.
- **Goal**: compile-time guarantee that tool name, args, and handler signatures stay aligned.
- **Constraint**: keep current tool schema payloads unchanged for clients.

## Execution Steps
1. Create strict mapped types from tool names to args/results.
2. Refactor dispatch utilities to generic-safe interfaces with no `any` fallbacks.
3. Update router tool listing/dispatch code to honor strong contracts without casts.
4. Adjust tests to assert type-safe behavior and runtime compatibility.

## Verification Requirements
- **Automated Tests**: `npm run test -- tests/backend/mcp/tool-registry.test.ts`; `npm run test -- tests/backend/mcp/mcp-tool-availability.test.ts`; `npm run typecheck`
- **Criteria**: No `any` in MCP contract/dispatch path.
- **Criteria**: Tool registry and router compile with strict generic typing.

## Engineering Standard
- Use the feature branch: feature/sprint2-implementation
- Ensure all tests pass before completing.
