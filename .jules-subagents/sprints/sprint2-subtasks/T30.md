title: Test, CI, and Documentation Drift Hardening
depends_on: [T06, T16, T21, T29]
is_independent: true
merged: false
prompt:
# Task Specification: T30 - Sprint 2

## Objective
Strengthen CI quality gates, split oversized tests, and update architecture docs to match current post-refactor module layout.

## Files to Modify
- `.github/workflows/ci.yml`: Extend to include typecheck, lint, test, build, coverage, and audit/security checks.
- `tests/backend/sprint/sprint-orchestrator.test.ts`: Split into domain-focused files.
- `tests/backend/mcp/core-tool-handler.test.ts`: Split by behavior groups.
- `docs/architecture/repository-map.md`: Update stale file/module references.
- `docs/architecture/system-overview.md`: Update architecture map and module sections.
- `docs/sprint-loop/atomic-loop.md`: Update orchestrator step/file references.

## Technical Details & Research Findings
- **Context**: CI and docs still lag behind current repository standards and structure.
- **Goal**: enforce high-confidence quality gates and keep documentation synchronized with codebase reality.
- **Constraint**: avoid disruptive test behavior changes while splitting suites.

## Execution Steps
1. Upgrade CI workflow to run all required quality checks.
2. Split largest backend test suites into targeted modules with shared builders as needed.
3. Update architecture/sprint-loop docs to remove stale paths and reflect real module map.
4. Run full local CI equivalent and capture results.

## Verification Requirements
- **Automated Tests**: `npm run ci`; `npm run test:coverage`; `npm run build`
- **Criteria**: CI pipeline enforces full quality gates.
- **Criteria**: Docs have no stale module references in updated architecture pages.

## Engineering Standard
- Use the feature branch: feature/sprint2-implementation
- Ensure all tests pass before completing.
