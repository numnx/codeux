⏸️ **Watch Loop Output Interval Reached**
- Elapsed: {{elapsed_seconds}} seconds
- Running tasks: {{running_tasks}}
- Pending tasks: {{pending_tasks}}
- Completed tasks: {{completed_tasks}}
- Failed tasks: {{failed_tasks}}

All currently active tasks are still in progress.

**Instruction:** Call `sprint_agent(action: "{{action}}", wait: true)` again to continue the watch loop.
