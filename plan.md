1. **Create the test file**: `tests/backend/domain/sprint/orchestrator/sprint-finalization-service.test.ts`.
2. **Setup Test Environment**:
   - Use `vi.mock` to mock `SprintFinalizationDependencies` and create `buildDeps()`. This includes exact mocks for `logger`, `completedSprints`, `renderInstruction`, `qualityAssuranceService`, `sprintIssueService`, `executionRepository`, `projectAttentionService`, `heartbeatService`, and `workspaceManager`.
3. **Implement 5 test cases for `SprintFinalizationService.finalize()`**:
   - **a. Normal completion**: Provide `subtasks` fully merged and complete (`status: "COMPLETED"`, `is_merged: true`, `worker_branch: "w"`, `pr_url: "p"`). Setup `renderMainMergeCiFeedback` spy to return `{ state: "ready_for_merge", hasMergeConflict: false, hasFailedChecks: false }`. Call `finalize()`. Assert `executionRepository.updateSprintRun` is called with `"completed"` (via `transitionSprintRun`).
   - **b. QA exhaustion**: Provide subtasks and configure `deps.qualityAssuranceService.reviewSprintCompletion` to return `{ blockedCompletion: false, reportText: "exhausted" }`. Assert `finalize()` transitions the sprint to `completed` and the report string includes "exhausted" (verifying it does not block completion).
   - **c. Main merge conflict**: Setup `renderMainMergeCiFeedback` spy to return `{ state: "merge_conflict", hasMergeConflict: true }`. Assert `finalize()` calls `deps.projectAttentionService.openItems` with a `merge_conflict` attention item, and returns `{ status: "wait" }` or `{ status: "exit" }` instead of completing the sprint.
   - **d. Issue closure**: Provide `subtasks` fully merged and complete. The code executes `sprintIssueService?.closeLinkedIssues(scopedExecutionContext.project.id, scopedExecutionContext.sprint.id)` unconditionally when all tasks are complete. Create a spy for `closeLinkedIssues` that asserts it has been invoked exactly once per normal completion, using a mock return value `{ reportText: "issues closed" }` and verify the report output when `issueId` is provided (`scopedExecutionContext.sprint.issueId = "123"`). Add a case verifying that when `issueId` is absent (`scopedExecutionContext.sprint.issueId = null`), `closeLinkedIssues` handles the check (or is not invoked).
   - **e. No-output task**: Provide a subtask with `status: "COMPLETED"`, `worker_branch: null`, `pr_url: null`. Ensure `needsManualMerge: false` in params. Assert `finalize()` treats this as settled and completes the sprint properly (calling `updateSprintRun` with `"completed"`).
4. **Execute Tests**:
   - `npx vitest run tests/backend/domain/sprint/orchestrator/sprint-finalization-service.test.ts`
   - `npm test`
5. **Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.**
