.jules-subagents/worktrees/
node_modules
dist
.env
.playwright-mcp/
.playwright-mcp/*
.DS_Store
server.log
.pnpm-store
coverage/
release/
.cache/
.jules-subagents/debug.log
.sprint-os/debug.log
.sprint-os/runtime/
.sprint-os/worktrees/
.sprint-os/worktrees/*
*.cjs
*.log
.code-ux/data.db
.code-ux-home/
.claude/
.antigravitycli*
logs/openai/
# Stray gitlink that recurs from running tools with HOME=~ inside the repo
/~/

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/
.qwen
# Local scratch files
/test-*
/tmp*

# Product video captures and renders may contain private local project names.
/public/product-video/captures/*.png
/public/product-video/captures/*.json
/public/product-video/clips/*.webm
/public/product-video/clips/*.json
/product-video/out/

# This is a pnpm-only project — never commit an npm lockfile (ungoverned by pnpm overrides)
package-lock.json
**/package-lock.json

# Editor/merge backup files should never live in source trees.
src/**/*.orig
dashboard/src/**/*.orig
